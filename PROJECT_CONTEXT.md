# DiplomaHub — Project Context

Paste this file's contents at the start of a new session (or point Claude at it) to restore full context without re-deriving everything from scratch.

## What this is

DiplomaHub (repo/site name "IBPeople") is a community platform for International Baccalaureate (IB) students, alumni, and educators. Three content types anchor the app: **Resources** (uploaded files or external links), **Articles** (long-form writing), and **Discussions** (community Q&A). Plus user **Profiles** and an **Admin panel**.

## Location — read this first

The actual project lives at **`/Users/nathan/Desktop/DiplomaHub`**. If a session's primary working directory is `/Users/nathan/Desktop/ibpeople`, that folder is essentially empty — the real code is in DiplomaHub, listed as an "additional working directory." Always operate there.

It's a git repo (branch `features` was active), with teammate branches `Arnav`, `Ben`, `Nathan`, `main`.

## Tech stack

- **Next.js 16.2.9** (App Router) — newer than Claude's training data. Check `node_modules/next/dist/docs/` before assuming an API differs from what you remember (AGENTS.md enforces this).
- **React 19.2.4**, TypeScript, **Tailwind CSS 4** (custom design tokens via `@theme inline` in `app/globals.css` — see "Design system gotcha" below, it matters more than it sounds).
- **Supabase**: Postgres DB, Auth (`@supabase/ssr`), Storage. Project ref: `gmfqdkbghbxdlxffwfca`. No migration files in the repo — schema changes happen by handing the user raw SQL to run in the Supabase SQL editor.
- **Tiptap v3** — rich text editor for Articles. **sanitize-html** — server-side sanitization. **lucide-react** for icons.
- `bootstrap`/`react-bootstrap` are in `package.json` but unused — UI is hand-built Tailwind.

## Design system

`app/globals.css` `@theme inline` block defines colors (`--color-primary/secondary/tertiary` + `-container`/`on-*` variants), spacing (`xs/sm/md/lg/xl/gutter/margin`), type scale (`label-sm/md`, `body-md/lg`, `headline-md/lg`, `display-lg`), fonts (`Inter` sans, `Merriweather` serif).

**Gotcha (cost real debugging time):** because the theme block uses `@theme inline` (not plain `@theme`), Tailwind bakes the *literal resolved value* into each generated utility class instead of keeping a `var(--text-display-lg)` reference. This means overriding `--text-display-lg` etc. in a later CSS rule **does nothing** — the utility classes already have `font-size: 3rem` hardcoded. To make type scale responsive, `globals.css` now has a `@media (max-width: 640px)` block that targets `.text-display-lg` / `.text-headline-lg` / `.text-headline-md` **by class name directly** (unlayered CSS beats Tailwind's `@layer utilities` regardless of source order, so no `!important` needed). Keep this in mind before trying to theme anything else via CSS variables in this file.

Article body typography is centralized in `.article-editor`/`.article-content` classes so the Tiptap editor and rendered article match.

## Data model (Supabase Postgres)

Tables: `users`, `resources`, `articles`, `discussions`, `discussion_replies`, `comments`, `likes`, `saved_items`, `feedback`, `notifications`, `roadmap_items`, **`hub_items`, `hub_study_log`** (new, 2026-09-16 session — see "Hub planner" section below).

Key columns:
- `users`: `display_name`, `is_pro`, `ib_year`, `avatar_url`, `bio`, `points`, `author_trust_score`, `last_active_at`, **`hub_subjects`** (text[], nullable — the Hub's onboarding subject picks; `null`/empty means "no picks, show everything"), **`hub_onboarded_at`** (timestamptz, nullable — drives whether the Hub onboarding wizard auto-opens; both new, 2026-09-16 session, **SQL not confirmed run**, see "Hub planner" below). `is_pro` and `author_trust_score` are **not** in the client-writable column grant — only the service-role client can update them (see Admin panel section); `hub_subjects`/`hub_onboarded_at` **are** client-writable (regular session client, scoped to the caller's own row).
- `resources`: title, description, subject_tag, type_tag, year_tag, file_url, **link_url** (new, 2026-07-30 — see below), author_id, download_count, like_count, published, community_trust. See "Community trust score formula" below for how `community_trust` is actually computed — it's not a simple weighted sum.
  - **A resource can now carry a file, a link, or both** (`link_url` added 2026-07-30, migration confirmed run and verified live) — the old `type_tag === "External Link"` either/or model is gone. `DownloadButton` takes a `kind: "file" | "link"` prop and can render twice on one resource (once per attachment present).
  - **`type_tag` retired the generic `"Exemplar"` value** (2026-09 session), split into `"Exemplar - IA"`, `"Exemplar - EE"`, `"Exemplar - TOK"` — lets a resource be "an exemplar" *and* "specifically about the EE/TOK, not tied to one subject" at the same time. The browse-page Resource Type filter still shows a synthetic **"Exemplar"** option that matches all three underlying values via `.in()` (see `resources.ts`'s `EXEMPLAR_TYPE_VALUES`) — picking it shows IA+EE+TOK together, picking a specific one narrows. Resources that were `subject_tag = 'EE'`/`'TOK'` but were actually about one real subject (title-parseable, e.g. "Chemistry EE Exemplar...") got re-tagged to that real subject + `type_tag = 'Exemplar - EE'`; `subject_tag = 'EE'`/`'TOK'` now means genuinely cross-subject content only (EE-process guides/templates; all TOK essays, since TOK isn't "really" any other subject). `"Past Paper Tips"` was also renamed to `"Past Paper"`.
  - **`subject_tag` gained `"Theatre"`** (pure `pills.tsx` addition, no data migration).
- `articles`: title, slug (uuid-suffixed, see below), content (HTML), cover_image_url, author_id, topic, view_count, like_count, published.
- `discussions`: title, content (plain text), author_id, subject_tag, type_tag, year_tag, reply_count, like_count, **top_reply** (uuid FK to `discussion_replies.id` — it is NOT the reply text, see gotcha below). No `published` column — always live.
- `comments`: polymorphic (resource_id/article_id), flat, no threading, no likes column.
- `discussion_replies`: polymorphic-adjacent to discussions, has `like_count` and `parent_reply_id` (reserved, unused — no threading UI).
- `feedback`: `id`, `user_id` (nullable — anonymous feedback allowed), `content`, `created_at`. **Insert-only RLS policy, no SELECT policy at all** — even the admin has to read it via the service-role client (see Admin panel).
- `notifications`: `id`, `user_id` (recipient), `actor_id` (nullable — null for system-triggered types like download milestones), `type`, `message`, `link`, `read`, `created_at`. **No INSERT grant at all** — a notification's `user_id` is always someone *other* than the caller, so it can't be expressed as an `auth.uid()`-scoped RLS policy; all inserts go through the service-role client from `createNotification()` in `app/lib/actions/notifications.ts`. SELECT/UPDATE are scoped to `user_id = auth.uid()` for the regular client (reading your own notifications, marking them read).
- `roadmap_items`: `id` (text slug, e.g. `"ib-news"`), `title`, `status` (`completed`/`in_progress`/`planned`), `completion_percentage` (nullable), `sort_order`, **`release_label`** (text, the human date string e.g. "Estimated Jan 2027"), **`description`** (text), **`tags`** (jsonb array of `{label, icon}`, new 2026-09 session). **Everything is now admin-CRUD-able** — see "Roadmap" section below, this replaced the old hardcoded-content model entirely. Same no-client-grant shape as `notifications` — all writes via service-role.

**Gotcha:** `discussions.top_reply` is a raw FK, not text. Every place that displays it (`getDiscussionsWithUserState`, `getPublicProfile`, `getSavedItems`) does a follow-up query to resolve the id → `discussion_replies.content` before returning it. If you add a new discussion-listing query, remember to do the same resolution or you'll render a UUID string in the "top reply" quote box.

**Gotcha:** `articles.slug` is derived from the title (`title.toLowerCase().replace(...)`) with a random 8-char suffix appended (`crypto.randomUUID().slice(0, 8)`) to guarantee uniqueness against the `slug unique` constraint — two articles with the same title used to collide and throw a raw Postgres error.

Storage buckets: `resources`, `avatars`, `article-covers` — public-read, path `${user.id}/${uuid}.${ext}` (avatars use a **fixed** filename `${user.id}/avatar.${ext}` with `upsert:true`, so `uploadAvatar()` appends a `?v=${Date.now()}` cache-busting query param to the stored URL, or every re-upload would keep showing the old cached image at the same URL).

### Community trust score formula (2026-07-27)

`resources.community_trust` is computed by the Postgres function `calculate_community_trust(target_resource_id)` (no `security definer`, plain `plpgsql`, triggered on the resource's own like/download changes and on the author's `author_trust_score` changing) — **not** the simple "40% author + 30% likes + 30% downloads" description that was in this doc before; that was stale. The real formula is a **Bayesian shrinkage blend**:

```
like_component     = least(100 * ln(1 + like_count) / ln(1 + 50), 100)       -- log-scaled, capped
download_component = least(100 * ln(1 + download_count) / ln(1 + 100), 100)  -- log-scaled, capped
community_score    = 0.4 * like_component + 0.6 * download_component
engagement_volume  = like_count + download_count
confidence         = engagement_volume / (engagement_volume + 20)           -- confidence_k = 20
blended            = confidence * community_score + (1 - confidence) * author_score
trust              = round(greatest(author_score, blended))                  -- floor added 2026-07-27, see below
```

**Bug found and fixed 2026-07-27**: before the floor, `trust = round(blended)` directly. Since `blended` is a weighted average that shifts toward `community_score` as engagement grows, **more downloads visibly lowered the trust score** whenever `community_score < author_score` — e.g. a new resource from a 90-trust author starts at `trust ≈ 90` (confidence ≈ 0 with no engagement yet), and as downloads come in the score drifts toward the resource's own (lower) engagement-derived score before eventually climbing past it once `community_score` itself overtakes `author_score`. Real, reproducible dip, not a data bug — confirmed against the live function body (`pg_get_functiondef('calculate_community_trust'::regproc)`).

**Fix**: `trust := round(greatest(author_score, blended))` — engagement can now only push the score up from the author's baseline, never below it. **SQL was handed to the user to run in the Supabase SQL editor; likely applied** (a follow-up report — "downloaded a 50-author-trust resource 10 times, still shows 50%" — is exactly the expected post-fix behavior, since `confidence_k = 20` means ~50 downloads are needed before `blended` even crosses back above a 50 baseline with zero likes) but **not explicitly confirmed run**. If `community_trust` is ever observed *decreasing* as likes/downloads increase, this fix hasn't landed — re-fetch the live function body before assuming it has.

**Open, unresolved as of 2026-07-27**: whether the current tuning (`confidence_k = 20`, log caps at like_count=50/download_count=100) is too insensitive — a resource can take dozens of downloads before `community_trust` visibly moves at all for a mid-trust author, which may or may not be the desired UX. Not changed pending the user's input; asked them to confirm the resource's actual `download_count` to rule out the trigger not firing at all, but the question was interrupted/dismissed — still open.

A different architecture was discussed and explicitly declined by the user: making `community_trust` pure engagement (no author blending at all) and deriving `author_trust_score` as an average of the author's own resources' scores (inverting today's one-directional dependency). Tradeoff flagged: it would make new resources start at 0% trust regardless of who posted them, and would make the existing admin-editable `author_trust_score` field in `/admin/users` pointless (would get overwritten on the next engagement change). **Decision: keep the current architecture** (author score as an independent admin-set prior, resources blend toward it) — just with the floor fix above.

### Retention tracking — `users.last_active_at` (2026-09 session, new)

Built to answer "how do I know if visitors come back" — Vercel's built-in Web Analytics (which the user enabled directly in the Vercel dashboard, see "Vercel Fluid CPU cost investigation" below) is deliberately privacy-first with no persistent visitor id, so it structurally can't report returning-vs-new visitors. For **logged-in** users this app already had a much better signal available for free.

Designed explicitly to add near-zero cost, given the session's whole CPU-cost context:
- `proxy.ts` gates the write on a cookie (`la-ping`, ~24h maxAge), not a DB read — so the throttle check itself never touches the database. A user with 50 page views in a day still costs exactly **one** write, not 50.
- The write itself runs via `event.waitUntil(...)` (Next's background-work API for middleware) so it never adds latency to the response the user is actually waiting on, and a swallowed `.catch()` means a missing column/grant fails silently rather than breaking navigation.
- Only fires for real navigations — the existing prefetch early-return in `proxy.ts` (see Auth section below) already skips this path for prefetches.

`app/admin/page.tsx` shows two new stat cards, "Active Users (7 days)" / "Active Users (30 days)", computed in `getAdminAnalytics()` from simple count queries against `last_active_at`.

**SQL needed (column + self-service grant), status unclear — verify before trusting the stat**:
```sql
alter table public.users add column if not exists last_active_at timestamptz;
grant update (last_active_at) on public.users to authenticated;
```
This was handed to the user during the session, but with heavy context-compaction across such a long session there's no explicit "ran it" confirmation on record. If "Active Users" shows 0 or looks obviously wrong, check this first.

## Auth & permissions

- `app/lib/supabase/server.ts` — cookie-based server client (respects RLS).
- `app/lib/supabase/admin.ts` — service-role client (bypasses RLS **and column grants**). Used for: download/view-count RPCs, and now also the admin panel's `author_trust_score`/`is_pro` writes and all `feedback` reads (both blocked for the regular client — column grants and missing SELECT policy respectively, not just RLS).
- `app/lib/get-current-user.ts` — **`getCurrentUserId()` (new, 2026-09 session) is now the default choice** for "is someone logged in / what's their id" — it reads the `x-verified-user-id` request header that `proxy.ts` already sets after validating the session, so it costs nothing (no Supabase call at all). `getCurrentUser()` (the original, full Supabase `User` object) still exists but now does its own real `supabase.auth.getUser()` network call — kept only for the one place that needs more than the id (`/profile/edit`'s `email_confirmed_at` check). Audited all 17 call sites in the app during the migration; 16 only ever used `.id`/truthiness and were switched to `getCurrentUserId()`, the 17th (`profile/edit`) stayed on `getCurrentUser()`. `getCurrentUserProfile()` (looks up the full `users` row) was also switched to call `getCurrentUserId()` internally instead of `getCurrentUser()` — this one matters most since it's rendered on **every page** via the root layout's navbar. Both remain `cache()`-wrapped. **If you add a new page that needs to know the current user, default to `getCurrentUserId()` unless you specifically need Auth-only fields.**
- `app/lib/resolveOrigin.ts` — **new**. Resolves the real public-facing origin from `x-forwarded-host`/`x-forwarded-proto` request headers instead of trusting `request.url` or an env var. Needed because `signInWithGoogle()`'s `redirectTo` used to be built from a static `NEXT_PUBLIC_SITE_URL` env var, so anyone signing in from a device other than the dev machine got redirected to the dev machine's `localhost` after Google auth completed. Used in `app/auth/actions.ts` and `app/auth/callback/route.ts`.
- **Admin bypass**: `app/lib/admin.ts` exports `isAdmin(userId)` checking `ADMIN_USER_IDS = ["c5680362-be56-490f-b6bc-471363da8648"]`. Every ownership check follows:
  ```ts
  const isOwner = currentUser?.id === row.author_id || isAdmin(currentUser?.id);
  ```
  Enforced both client-side (show/hide Edit/Delete) and server-side (skip the `.eq("author_id", ...)` filter for admins). Admin panel server actions (`app/lib/actions/admin.ts`) additionally re-check `isAdmin()` themselves — never rely on the `/admin` layout guard alone.
- **Login page auto-redirects if already logged in** — `app/login/page.tsx` is now a server component that checks `getCurrentUserId()` (see above) and `redirect("/")`s before rendering; the actual form was extracted to `components/login/LoginForm.tsx` (client component).
- **`proxy.ts` (root-level middleware, renamed from `middleware.ts`/`middleware()` per Next's deprecation) rewritten twice more in the 2026-09 session, purely for Vercel Fluid CPU cost — see the dedicated section below for the full story.** Net effect: it now verifies the session via `supabase.auth.getClaims()` (local JWT signature check against a cached JWKS, no network round trip) instead of `getUser()` (a real Auth-server call on every request), skips verification entirely for Next.js Link-prefetch requests, forwards the result downstream via a request header (`x-verified-user-id`, always server-set so it can't be spoofed) for `getCurrentUserId()` to read for free, and does the throttled `last_active_at` background write described above. If you're debugging anything auth-timing-related, this file is the first place to check — it's had more churn than anywhere else in the app this session.
- **VS Code port-forwarding / tunneling**: `next.config.ts`'s `experimental.serverActions.allowedOrigins` includes `"**.devtunnels.ms"` and `"localhost:3000"` — VS Code's forwarding proxy injects an `x-forwarded-host` of the tunnel domain on *every* request through that port, even ones loaded via plain `localhost`, which otherwise trips Next's Server Actions CSRF check ("Invalid Server Actions request"). If the tunnel domain changes (new VS Code session, no persistent tunnel configured), this needs updating, and so does Supabase's **Redirect URLs** allowlist (Authentication → URL Configuration) for OAuth to keep working from that domain.
- **Forgot-password / reset-password flow (2026-07-23, new — there was previously no way to recover a forgotten password at all)**: `/forgot-password` (`ForgotPasswordForm.tsx`) calls `requestPasswordReset(email)` in `auth/actions.ts`, which always reports success regardless of whether the email is actually registered (prevents user enumeration). Supabase emails a link to `/auth/reset-password` (a route handler, not a page — mirrors `/auth/callback`'s shape, since exchanging the code for a session requires setting cookies, which a Server Component can't do during render). That handler exchanges the code and redirects to `/reset-password` (the actual page, `ResetPasswordForm.tsx`), which calls `updatePassword(password)`. Rate-limited via the `auth` bucket, same as signin/signup. See the "same root cause bit us twice" note above for a real bug this flow hit in production (Supabase Redirect URLs allowlist).
- **Signup now has a confirm-password field** (client-side match check before calling `signUp()`, same pattern as `ResetPasswordForm.tsx`). Also fixed a real layout bug shared by the login and signup forms: the password `<input>` looked narrower than the email input because it shared its flex row with the eye-icon toggle button and used `w-full` (which computes against the row, not the field) — now `flex-1 min-w-0`, and on the login form specifically the email row got an invisible spacer button matching the eye-icon's footprint so the two fields visually align exactly.

### RLS/grants gotcha (bit us more than twice now)
`resources` had proper GRANT + RLS from the start. `articles` and `discussions` did not initially — same fix pattern:
```sql
grant usage on schema public to authenticated, anon;
grant select, insert, update, delete on public.<table> to authenticated;
grant select on public.<table> to anon;
-- plus RLS policies scoped to author_id = auth.uid() (or the admin id)
```
Separately, **column-level grants** restrict which fields are writable even when RLS allows the row update — `users.is_pro`/`users.author_trust_score` are deliberately excluded from the client grant (only `display_name`/`ib_year`/`bio`/`avatar_url` are writable by `authenticated`). If a new admin-only field needs writing, it needs the **service-role client**, not just an `isAdmin()` check with the regular client — the regular client will get a real Postgres permission error regardless of RLS.

## Shared taxonomy — `components/pills.tsx`

Single source of truth for subject/type/year options:
- `SubjectTags`/`ActiveSubjectTags`: Math AA, Math AI, Physics, Chemistry, Biology, English, French (added 2026-07-23), History, Economics, Geography, Business, **Theatre** (added 2026-09 session, same tertiary colors as English/French/History/Economics/Geography/Business), TOK, EE, General, CAS.
- `ResourceTypeTag`: **reordered so the Exemplar variants show first** (2026-09 session, at the user's request) — `Exemplar - IA`, `Exemplar - EE`, `Exemplar - TOK`, Template, Guide, Subject Notes, Past Paper, Other. Was `Template, Guide, Subject Notes, Past Paper Tips, Exemplar, External Link, Other` before this session — `External Link` is gone entirely (see the `resources.link_url` note in Data model), `Past Paper Tips` → `Past Paper`, and the single `Exemplar` split three ways (see Data model for why). The Resources browse-page filter dropdown (`resources-grid.tsx`) inserts one extra synthetic `"Exemplar"` option right before the three specific ones — not a real `type_tag` value, it expands to `.in(["Exemplar - IA","Exemplar - EE","Exemplar - TOK"])` server-side.
- `TitleTag`: IB Educator, IB Student, Alumni — rendered via `ibYearTitleTag(ib_year)`, which maps the real `users.ib_year` values (`Pre-IB`/`DP1`/`DP2`/`Alumni`/`Educator`) to these three labels. **Was fixed-width (`w-25`, 100px) and "IB Educator" didn't fit — now sizes to content** (`whitespace-nowrap` + padding).
- `YEAR_OPTIONS`: Whole DP, Pre IB, Post IB. **DP1/DP2 removed 2026-07-27** at the user's request ("clearer, wasn't necessary") — this is for resource/discussion `year_tag` categorization specifically, a totally different field from `users.ib_year` (Pre-IB/DP1/DP2/Alumni/Educator, the student's own status used at signup/profile — untouched, still has DP1/DP2). User had already manually reassigned every existing DP1/DP2-tagged resource/discussion to "Whole DP" in the DB before this change, so no data migration was needed — this was purely a `components/pills.tsx` constant edit, and every dropdown/filter/validator that reads `YEAR_OPTIONS` picked it up automatically (see "Rule" below).

**Rule:** every dropdown/filter does `Object.keys(SubjectTags)` etc. rather than a local array. Edit `pills.tsx` once to add a new option everywhere.

## Engagement features — comments, likes, saves, avatars (all real now)

This was previously the biggest gap in the app; it's now fully wired end to end.

- **`components/likeButton.tsx` / `components/saveButton.tsx` / `components/shareButton.tsx`** — shared, reused everywhere an author or engagement affordance is shown. All three: optimistic local update on click, **reconciled against the server's actual response** if it disagrees (handles stale initial state gracefully instead of drifting further out of sync — this was a real bug on the homepage before the fix). Active/liked state color is set via inline `style` (not a Tailwind class) because appending a color class after a base class with its own color utility is unreliable — Tailwind's generated CSS order (not DOM class order) decides which wins, so the "liked" color sometimes silently lost to the base gray. `ShareButton` copies `window.location.href` and shows a `bg-secondary-container text-secondary` "Copied!" pill for 2s.
- **`components/avatar.tsx`** — renders the real `avatar_url` (circular, `object-cover`) or falls back to initials (`app/lib/initials.ts`). Used everywhere an author is shown (resource/article/discussion cards and detail pages, comments, profile page, admin Users table, community "Your Status"/leaderboard). Replaced generic `CircleUser`/`Users`/`User` lucide icons that used to stand in for real photos.
- **`components/detailed-articles/comments.tsx` + `comment.tsx`** — single shared component pair, takes a `kind: "comment" | "reply"` prop. `kind="comment"` posts via `addComment`/`deleteComment` (resources/articles, `comments` table, no likes/threading — schema doesn't support it, so no like button or reply affordance is shown). `kind="reply"` posts via `replyToDiscussion`/`deleteReply` (discussions, `discussion_replies` table, **does** support likes so a real like button shows per-reply). Delete is owner-or-admin gated, confirm()-guarded, calls `router.refresh()` after.
- Every `*WithUserState`/detail-fetch action (`getResourcesWithUserState`, `getArticlesWithUserState`, `getDiscussionsWithUserState`, `getResourceDetail`, `getArticle`, `getDiscussion`, `getSavedItems`, `getPublicProfile`) now attaches real `isLiked`/`isSaved` for the *current viewer* (not the content owner) via batched `.in()` queries against `likes`/`saved_items`.
- Views: **articles only**, already real before this pass (`increment_view_count` RPC fired in `getArticle()`). Resources/discussions have no `view_count` column by design — confirmed with the user, not a gap.

## Admin panel (`/admin`) — new

Guarded by `app/admin/layout.tsx` (`redirect("/")` if `!isAdmin()`), tab nav to four sections. An "Admin" link appears in the nav dropdown (`components/profileMenu.tsx`) only when `isAdmin(id)` is true.

- **Dashboard** (`/admin`) — counts: users, resources, articles, discussions, unpublished resources/articles.
- **Users** (`/admin/users`, `components/admin/UsersTable.tsx`) — paginated (10/page, server-side search on `display_name`/`email` via `getUsersPage()`, 2026-07-27), inline-editable `author_trust_score` (0–100) and an `is_pro` toggle (both write through the **service-role client**, see column-grants gotcha above), and a **Delete** button per row (`deleteUserAsAdmin()`, 2026-07-27 — see "Account deletion" below) that's hidden/blocked for any `isAdmin()` account including the caller's own.
- **Content** (`/admin/content`, `components/admin/ContentTable.tsx`) — tabbed resources/articles/discussions, each tab paginated server-side (10/page, search on `title` via `getAdminResourcesPage`/`getAdminArticlesPage`/`getAdminDiscussionsPage`, 2026-07-27 — the tab-count badges come from a separate lightweight `getAdminContentCounts()` head-count query so switching tabs doesn't refetch all three full row sets), publish/unpublish toggle, delete (reuses the *existing* `deleteResource`/`deleteArticle`/`deleteDiscussion` actions — they already had admin-bypass logic, there was just no UI to reach someone else's content before).
- **Feedback** (`/admin/feedback`, `components/admin/FeedbackTable.tsx`) — paginated (10/page, server-side search on `content` via `getFeedbackPage()`, 2026-07-27), service-role client since that table has no SELECT policy for anyone, delete.
- **Roadmap** (`/admin/roadmap`, `components/admin/RoadmapTable.tsx`) — edit each roadmap card's status and (when status is "In Progress") completion percentage. See "Roadmap" section below.

**Pagination note (2026-07-27)**: all three tables above used to fetch every row unbounded and filter/search entirely client-side — the same anti-pattern the 2026-07-21 hardening pass fixed for the public resources/articles/community grids, just missed here since the admin panel didn't exist yet at that point. Rewritten to the same `get<Thing>Page(filters)` + debounced-fetch pattern (see "Established code patterns"). Server-side search only matches columns on the table itself (not joined author names, unlike the old client-side filter) — a deliberate scope trade-off, see the resources/articles/discussions functions in `app/lib/actions/admin.ts` if that's ever missed. **Not manually click-tested by Claude** (no admin login credentials available) — verified via clean `tsc`/`eslint` and a clean dev-server compile only; worth a manual pass, especially the tab-switch + search + pagination interaction on Content.

**Account deletion (2026-07-27)** — both self-service and admin-triggered:
- Self-service: a "Danger Zone" section on `/profile/edit` (`ProfileForm.tsx`) → `deleteAccount()` in `app/auth/actions.ts`, which uses the service-role client's `auth.admin.deleteUser()` (there's no client-facing delete-own-account API), signs out, redirects home.
- Admin: a Delete button per row in `/admin/users` → `deleteUserAsAdmin()` in `app/lib/actions/admin.ts`, same `requireAdmin()` pattern as everything else in that file. Refuses to delete any hardcoded `isAdmin()` account (including the caller's own) since `ADMIN_USER_IDS` is a fixed array in source — deleting that auth user would brick admin access until a redeploy, not remove them from the list.
- Both rely on new `ON DELETE CASCADE` foreign keys from `resources`/`articles`/`discussions`/`discussion_replies`/`comments`/`likes`/`saved_items`/`notifications.user_id` down to `public.users.id` — user chose **full deletion** of a departing user's content (not anonymization/"Deleted user" placeholder) when asked. `notifications.actor_id` and `feedback.user_id` deliberately got `ON DELETE SET NULL` instead — both were already nullable by design (system notifications, anonymous feedback), and cascading them would wrongly delete *other* users' still-valid notifications or destroy useful feedback content just because its submitter left. **SQL was handed to the user to run; not explicitly confirmed applied** — if account deletion ever fails with a foreign-key-violation error, this is why, check whether it was actually run.

**Admin UPDATE RLS policies (2026-07-27, confirmed run)** — `resources`/`articles`/`discussions` each got a second, additive permissive UPDATE policy ("Admins can update any X") allowing the hardcoded admin UUID regardless of `author_id`, alongside (not replacing) the existing owners-only policy — Postgres OR's multiple permissive policies for the same command together. Closes a real gap: `updateResource`/`updateArticle`/`updateDiscussion` already skipped the `.eq("author_id", ...)` filter for admins in application code, but until this ran, RLS itself silently returned zero rows for an admin editing someone else's content, surfacing as "You can only edit your own X" even for admins.

Users and Content tables: the user/author cell links out to `/profile/[id]`, and Content's title links to the actual resource/article/discussion page (articles resolve via `slug`, which had to be added to the admin query — it wasn't being fetched before).

All admin server actions live in `app/lib/actions/admin.ts` behind an internal `requireAdmin()` helper that re-derives the current user and checks `isAdmin()` itself, independent of the layout guard.

`award_points()`'s auto `is_pro` upgrade trigger fires at **1000 points** — confirmed run by the user (was previously 500; UI copy already assumed 1000 before the SQL was confirmed applied).

## Feedback (`/feedback`) — new

Simple textarea form (`app/feedback/page.tsx`), `submitFeedback()` in `app/lib/actions/feedback.ts` (works logged-in or anonymous, `user_id` nullable). Roadmap page's "Submit Feedback" button links here. The `feedback` table + RLS **has been created** by the user (confirmed) — see Admin panel section for the table's odd insert-only-no-select policy shape.

## Notifications (bell icon in navbar) — new

In-app + (currently disabled) email notifications when someone interacts with your content, all defined in `app/lib/actions/notifications.ts`.

- **Triggers**: likes on your resource/article/discussion/reply, comments on your resource/article, replies to your discussion, admin trust-score changes and Pro upgrades (manual admin action only — *not* the automatic 1000-point trigger), and resource download milestones (10/25/50/100/250/500/1000/2500/5000/10000 downloads, fires once per threshold crossed, no `actor_id` since it's not a directed action).
- **UI**: `components/notifications/NotificationBell.tsx` in the navbar — unread badge, dropdown of recent notifications, mark-read/mark-all-read, polls `getUnreadNotificationCount()` every 30s **while the tab is visible** (2026-09 session — was unconditional before; now pauses entirely via the Page Visibility API when the tab is hidden/backgrounded, and does one immediate catch-up poll when it becomes visible again — see "Vercel Fluid CPU cost investigation" below for why). No realtime/websockets.
- **Email**: sends via Resend (`RESEND_API_KEY` in `.env.local`), but **`EMAIL_SENDING_ENABLED = false`** is hardcoded in `notifications.ts` — the user's Resend account couldn't send to arbitrary recipients until domain verification cleared, originally on hold until **2026-09-02**. **That date has now passed (today is 2026-09-15)** — this wasn't revisited during the 2026-09 session (it was entirely focused on other things), so check with the user whether Resend verification actually cleared and it's safe to flip the flag now, rather than assuming either way. Still a deliberate code flag, not a date-check, so it hasn't silently started sending on its own.
- **Security note**: the email HTML body interpolates user-controlled text (display names, content titles) — `escapeHtml()` in `notifications.ts` guards against HTML injection into the email. Don't remove that when re-enabling email.
- Self-notifications are suppressed (`actorId === userId` → no-op) except download milestones, which have no actor.

## Roadmap (`/roadmap`) — fully DB-driven as of 2026-09 session (no more hardcoded copy)

Originally fully hardcoded, then (2026-07-27) status/completion-percentage only came from the DB. **As of the 2026-09 session, every field is DB-driven and admin-editable** — title, release-date label, description, tags, status, completion %, sort order — nothing lives in the React source anymore (the old `ROADMAP_CONTENT` map in `app/roadmap/page.tsx` is gone).

- `/admin/roadmap` (`components/admin/RoadmapTable.tsx`) now supports full CRUD, not just edit: create a new roadmap item, edit every field on an existing one, delete one. Tags are edited via a repeatable label+icon-picker UI; the icon picker is a closed dropdown of names from `app/lib/roadmapTagIcons.ts` (a plain string array, deliberately kept free of any `lucide-react` import so this server-safe validation file doesn't pull icon components into the server action bundle) — `components/roadmap/icons.ts` maps those same names to actual `lucide-react` components for rendering on the public page. A typo'd icon name can't happen since it's a dropdown, not free text.
- The public `/roadmap` page sorts fetched items **completed → in_progress → planned** (ties broken by admin-set `sort_order`) and shows only the **first 3** after that sort — the "what's done, what's active, what's next" framing the user asked for, and future-proofed for whenever there are more than 3 items in the table.
- New items get an id slugified from the title + an 8-char random suffix, same collision-avoidance pattern as article slugs.

SQL to add the three new columns (`release_label`, `description`, `tags jsonb`) and backfill the existing 3 rows' prior hardcoded copy was handed to the user and confirmed run.

## Search — resources, articles, community (2026-09 session, new)

Keyword search, not substring search: the query is split on whitespace and every word must appear in the title, in any order (`"IA Chemistry"` matches "Chemistry IA Template"). Shared helper `splitKeywords()` in `app/lib/validation.ts`, chained as `.ilike("title", "%word%")` per word (Supabase/PostgREST ANDs chained filter calls automatically) for Resources and Articles.

- **Resources/Articles**: plain code change, `getResourcesPage`/`getArticlesPage` gained a `search` filter, `resources-grid.tsx`/`article-grid.tsx` gained a debounced search input (reuses the existing 300ms-debounce pattern, see "Established code patterns").
- **Community/Discussions**: harder, since sorting/pagination there already goes through the `get_discussions_page`/`count_discussions` Postgres RPCs (see the 2026-07-21 pagination work) rather than a plain `.order()`. Both RPCs gained a new **trailing** `p_search text default null` parameter (appending rather than replacing existing params — Postgres's `CREATE OR REPLACE FUNCTION` only replaces a function if the argument *types* match exactly; a genuinely different parameter list creates a second overloaded function instead of replacing the first). **This bit us for real**: the first version of this fix used `CREATE OR REPLACE` naively and left the *old* 6-arg/3-arg versions of both functions still present alongside the new 7-arg/4-arg ones, which made PostgREST unable to pick a candidate for the call sites (`getTrendingDiscussions()`) that don't pass `p_search` — `"Could not choose the best candidate function"` error, live in production. Fixed by explicitly `DROP FUNCTION`-ing the old-signature versions once the new ones existed, plus a `NOTIFY pgrst, 'reload schema'` for good measure. **If you ever need to add a parameter to a Postgres RPC function again, drop the old-signature version explicitly — don't assume `CREATE OR REPLACE` alone removes it.**
- Search matching inside the RPC uses `title ilike all (array(select '%'||w||'%' from unnest(regexp_split_to_array(trim(p_search), '\\s+')) as w))` — same keyword-AND semantics as the plain-table version, expressed as a SQL array check since a Postgres function can't easily reuse the TS `splitKeywords()` helper.
- **Performance note (asked about by the user, answered, not acted on)**: `ilike '%word%'` can't use a normal btree index (leading wildcard), so it's a sequential scan per keyword — irrelevant at this app's current scale (~100-200 rows) but would want a `pg_trgm` GIN index on `title` if the table grows into the thousands. Not built pre-emptively.

## Leaderboard, profile rank, and "points are clickable everywhere" (2026-09 session, new)

- **`/leaderboard`** — new page, full site-wide ranking by `points` (not just the top-3 the Community sidebar already showed), paginated + searchable by name (`getLeaderboardPage()` in `app/lib/actions/profile.ts`, same `get<Thing>Page` pattern as everywhere else). Highlights the viewer's own row ("(You)" + a ring around their avatar) when logged in. Linked from Community's "Top Contributors" box and from a new "Top Contributors" section on the homepage (top 3, same data source as the Community sidebar's `getTopContributors()`).
- **Every place `points` is displayed now links to `/leaderboard`** (navbar profile dropdown, admin Users table, Community's "Your Status" widget, profile pages) — done in response to an explicit user request, not a design assumption. One structural note: the navbar profile-menu trigger used to be one giant `<button>` wrapping the name+points+chevron; wrapping just the points substring in a nested `<Link>` would have been invalid HTML (a `<button>` can't contain interactive content), so the points text was pulled *out* of the button into its own sibling `<Link>`, changing the row's DOM shape slightly — clicking the name/chevron still opens the dropdown as before, clicking the points text now navigates directly.
- **Profile pages show the viewer's numeric rank** (`"1,128 points · Rank #1"`, itself a link to `/leaderboard`) — computed in `getPublicProfile()` as `(count of users with more points) + 1`, a real query, not an estimate.

## Vercel Fluid CPU cost investigation (2026-09 session — significant chunk of this session)

The user hit **100% of the Hobby-plan Fluid Active CPU quota** (4h/period) with the site still up, prompting a real investigation rather than a guess-and-check pass. Diagnosed using the Vercel dashboard's own breakdowns (Type/Runtime/Projects tabs) rather than assumption, and corrected course at least once when data contradicted an earlier hypothesis — worth reading in order if this comes up again, since the *wrong* theories are as informative as the right one:

1. **First hypothesis (partially right, real fix, but not the dominant cause): `next.config.ts`'s `headers()` was matching `/(.*)`** — every request including every static JS/CSS/font/`/_next/image` request, not just page loads. Fixed by scoping the `source` pattern to exclude `_next/static`, `_next/image`, `favicon.ico` (verified live via `curl` that a real page still gets the CSP header, a static chunk doesn't). Real, but the Type breakdown later showed `middleware` and `function` invocation counts almost exactly 1:1 (not the 10:1+ you'd expect if middleware were mainly catching sub-resources), which pointed at something else being the dominant driver.
2. **Second fix: `NotificationBell`'s 30-second poll ran for every open tab regardless of visibility.** A background/idle tab has no user to notify — this was pure waste scaling with "tabs left open," not real usage. Fixed with the Page Visibility API: pauses the interval entirely while `document.visibilityState !== "visible"`, does one immediate catch-up poll when it becomes visible again. Verified the start/stop/no-double-interval logic in an isolated Node simulation (no way to visually test without login credentials).
3. **The real dominant cause, found by actually reading `proxy.ts` (which had been missed earlier in the session — searched for a file named `middleware.ts`/`middleware.ts` and concluded none existed, without checking for the renamed `proxy.ts` convention at the repo root):** the middleware called `supabase.auth.getUser()` (a real network round trip to Supabase's Auth server) on **every** page navigation, and separately, `getCurrentUser()` — called by `AuthNav` in the root layout, i.e. on **every single page** — did its own independent `getUser()` call. Two full Auth-server round trips per page view, for every visitor including anonymous ones. This directly explained the earlier 1:1 middleware:function invocation ratio (both were doing the same work, once each). Fixed by having `proxy.ts` forward its already-validated result downstream via a request header for `getCurrentUserId()` to read for free — see the Auth section above and the `proxy.ts` entries there for the full mechanism, including the later further optimization to `getClaims()` (local JWT verification) and skipping prefetch requests entirely, both done by the user directly between turns on their own further passes at the same file.
4. **Tried, then explicitly reverted at the user's request**: `@vercel/analytics` + `@vercel/speed-insights` were added to the root layout for traffic analytics, which required also updating the CSP to allow `va.vercel-scripts.com`/`vitals.vercel-insights.com` (they don't load same-origin, contrary to an initial assumption — caught via a live CSP-violation console error in the dev browser). **The user enabled Web Analytics directly in the Vercel dashboard instead** and asked for the package/CSP changes to be reverted — confirmed reverted to exact zero diff against the pre-change `layout.tsx`/`next.config.ts`/`package.json` before committing anything else. **If "add analytics" comes up again, check whether Vercel's dashboard-native Web Analytics is already enabled before reaching for the npm package** — it may already be handled outside the codebase.

**Verification note**: none of items 1–3 could be confirmed to have actually reduced the Vercel dashboard's numbers within this session (the usage charts only reflect full days *before* the fix), so treat "this should help" as reasoned-but-unverified until a subsequent session can compare a full day's numbers before vs. after.

## Admin analytics dashboard (`/admin`) — significantly expanded, 2026-09 session

`/admin`'s dashboard went from 4 plain count cards to a real analytics page — `getAdminAnalytics()` in `app/lib/actions/admin.ts`, `components/admin/charts/{LineChart,BarChart}.tsx`.

- **New stat cards**: Total Downloads, Total Resource Likes, Unpublished Resources, Unpublished Articles (the last two were already being computed by `getAdminStats()` but never actually rendered — a real pre-existing gap, not new data), plus Active Users (7/30 days) from the `last_active_at` work above.
- **Charts**: "New Content" (resources/articles/discussions created per day, last 30 days, one line chart), "New Users" (signups per day), "Most Downloaded Resources" (top 8, bar chart), "Resources by Subject" / "Resources by Type" (distribution bar charts).
- **Built as hand-written SVG components, not a charting library** — no `recharts`/`chart.js`/etc. was added, to avoid new bundle weight given the session's CPU/perf-consciousness. Used the `dataviz` skill's methodology: the app's exact brand hex values (`--color-primary` etc.) **fail** the categorical-chart-safety checks (`validate_palette.js` — too dark / too low-chroma to read as distinct series), so the charts use a 3-color palette *derived from* the brand hues but independently validated (`#2f53ce`/`#00a294`/`#c9820a` — passes all 6 checks: lightness band, chroma floor, CVD separation, normal-vision floor, contrast). Don't reuse the raw `--color-primary`/`--color-secondary`/`--color-tertiary` values directly for any future chart — they're for UI chrome, not proven safe as chart-series colors.
- **Real limitation, not a bug to "fix"**: there's no way to chart "downloads over time" — `download_count` is a running total column with no timestamped event log, so only content *creation* events (which have `created_at`) can be bucketed by day. Flagged to the user as a separate future decision (a new event-log table) rather than something to build speculatively now.
- **Not visually verified by Claude** — same standing constraint as the rest of the admin panel all session, no admin login credentials available. Verified via `tsc`/`eslint` plus isolated Node tests of the pure data-transformation logic (`niceMax()`'s clean-tick-number rounding, `bucketByDay()`'s zero-fill-and-sum behavior) — both confirmed correct in isolation, but the actual rendered charts have never been seen by Claude.

## Hub planner (`/hub`) — new, 2026-09-16 session

A brand-new feature built end-to-end this session: a personal weekly planner (drag/resize calendar, all-day milestones bar, Pomodoro focus timer, study-time log) replacing what used to be the `/community` nav slot for logged-in-feeling personal use. **`/community` itself, its components, and its DB tables are untouched** — only `components/Navbar.tsx`'s nav link was changed from "Community"/`/community` to "Hub"/`/hub`.

Built in two passes: first as a pure client-side design prototype (mock data, no DB), then wired to real Supabase persistence plus a first-run onboarding wizard once the design was approved. Both passes are done; **only the SQL below is unconfirmed as run** (this repo's standing "no migrations, SQL handed to the user by hand" convention — see below for the exact statements).

### Data model
- **`hub_items`**: one row per calendar task/IB-assessment/study-block/university-deadline. `id` is a **client-generated text id** (`itm-${crypto.randomUUID().slice(0,8)}`, same pattern as `articles.slug`/`roadmap_items.id`), not a DB-generated uuid — lets the client create optimistically with the final id already known, and the same id scheme is reused for guest localStorage rows that later get migrated in. `owner_id` FK to `auth.users`, `on delete cascade`. `subject_id` is a `text` CHECK-constrained to the 16-value `SubjectId` union in `components/hub/mock-data.ts` (see "Subjects" below — **if that union ever changes again, the DB check constraint needs updating too**, they're not automatically kept in sync). RLS: `owner_id = auth.uid()`, full CRUD grant to `authenticated`, **no grant to `anon` at all** — guests never touch this table, their data lives entirely in `localStorage` until they sign in (see "Guest mode" below).
- **`hub_study_log`**: one row per `(owner_id, log_date)` — a running per-day hours counter incremented by the Study Time card's quick-log buttons (+15m/+30m/+1h/+2h), not folded into `hub_items` since it's a counter, not a calendar task (would need fake titles/types and would pollute calendar-range queries). Same RLS/grant shape as `hub_items`.
- **`users.hub_subjects`** (text[], nullable) and **`users.hub_onboarded_at`** (timestamptz, nullable) — see "Data model" section above for the summary; full context in "Onboarding" below.

**SQL to run** (all three pieces, **not yet confirmed applied**):
```sql
create table if not exists public.hub_items (
  id text primary key,
  owner_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  type text not null check (type in ('ib_component','task','study_block','university')),
  subject_id text null check (subject_id in ('math_aa','math_ai','physics','chemistry','biology','english','french','history','economics','geography','business','theatre','tok','ee','general','cas')),
  start_at timestamptz not null,
  end_at timestamptz not null,
  all_day boolean not null default false,
  status text not null default 'todo' check (status in ('todo','done')),
  stages jsonb not null default '[]'::jsonb,
  notes text not null default '',
  resource_ids text[] not null default '{}',
  weight_label text null,
  created_at timestamptz not null default now()
);
alter table public.hub_items enable row level security;
grant usage on schema public to authenticated;
grant select, insert, update, delete on public.hub_items to authenticated;
create policy "Users manage their own hub items" on public.hub_items
  for all using (owner_id = auth.uid()) with check (owner_id = auth.uid());
create index if not exists hub_items_owner_start_idx on public.hub_items (owner_id, start_at);

create table if not exists public.hub_study_log (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,
  log_date date not null,
  hours numeric(4,2) not null default 0,
  created_at timestamptz not null default now(),
  unique (owner_id, log_date)
);
alter table public.hub_study_log enable row level security;
grant select, insert, update, delete on public.hub_study_log to authenticated;
create policy "Users manage their own study log" on public.hub_study_log
  for all using (owner_id = auth.uid()) with check (owner_id = auth.uid());

alter table public.users add column if not exists hub_subjects text[];
alter table public.users add column if not exists hub_onboarded_at timestamptz;
grant update (hub_subjects, hub_onboarded_at) on public.users to authenticated;
```
If `hub_items`/`hub_study_log` don't exist yet and someone signs in on `/hub`, the fetch actions (`getHubItems`/`getHubStudyLog` in `app/lib/actions/hub.ts`) will error — `app/hub/page.tsx` treats a failed fetch as an empty list rather than crashing the page, so it degrades to "looks like a brand-new empty account" rather than a hard error, but persistence silently won't work until the SQL runs.

### Subjects — 16, matching real resource tags exactly, capped at 6 + 4-core picks
`components/hub/mock-data.ts`'s `SUBJECTS`/`SubjectId` were deliberately kept in **exact 1:1 correspondence** with `components/pills.tsx`'s real `SubjectTags` taxonomy (Math AA, Math AI, Physics, Chemistry, Biology, English, French, History, Economics, Geography, Business, Theatre, TOK, EE, General, CAS) — `Subject.name` is spelled identically to the real `subject_tag` string, so `app/lib/actions/hub.ts`'s `getHubRecommendedResources()` can query real resources by subject with zero mapping table (`getResourcesPage({ subject: subject.name, ... })` directly).

`components/hub/onboarding/subject-cap.ts` is the single source of truth for the pick constraint: **TOK, EE, CAS, and General are core — pre-checked by default, free, and excluded from the cap** (every IB Diploma student has TOK/EE/CAS; General was included as a default too at the user's explicit request even though it isn't a real DP requirement, just a catch-all resource tag). The other 12 subjects are capped at **6 picks** (`MAX_SUBJECTS`), matching a real DP course load. `toggleWithCap()` silently refuses a 7th non-core pick; the picker UI (`step-pick-subjects.tsx`) visually disables (40% opacity) whatever's left once you're at 6, with a running "N of 6 selected" counter.

**This picked/capped set is what actually renders** — `components/hub/hub.tsx`'s `mySubjects` (derived via `computeMySubjectIds()`) drives both the Milestones bar's filter chips (`components/hub/milestones-bar.tsx`) and the Add Item dialog's subject dropdown (`components/hub/add-item-dialog.tsx`), **not** the full 16-subject list — deliberately, since `activeSubjectIds` (the calendar's active-filter state) is seeded from the same set, and an item whose subject isn't in that set would silently never render on the calendar (filtered out with no chip to toggle it back on). If `hub_subjects` is null/empty (not yet onboarded, or explicitly skipped), `mySubjects` falls back to all 16.

**Subjects are editable after onboarding** — a "✎ Edit subjects" button next to the filter chips opens `components/hub/onboarding/edit-subjects-dialog.tsx` (same picker, same cap, standalone modal, no tour) and re-saves via the same `completeHubOnboarding()` action/localStorage key as the wizard.

### Onboarding wizard — 4 steps, spotlight-animated, guest-aware
`components/hub/onboarding/onboarding-wizard.tsx` + `step-{welcome,calendar,timer-milestones,pick-subjects}.tsx`. First real product-tour UI in this codebase (previous modals — `add-item-dialog.tsx` — were plain centered dialogs; this reuses that shell for step 1 and the Edit Subjects dialog, but steps 2–4 do something new).

- **Spotlight effect, pure CSS, no animation library**: each step (except step 1, "Welcome") targets real page elements via `data-tour="..."` attributes (`week-calendar.tsx`, `focus-timer-card.tsx`, `milestones-bar.tsx`'s root and its inner filter-chip row specifically). A `box-shadow: 0 0 0 9999px rgba(...)` on a div sized/positioned exactly over the target creates the dimmed-everywhere-except-here cutout; its `top/left/width/height` are plain CSS-transitioned values, so the spotlight (and the step panel, anchored next to it) glides smoothly as you click Next/Back — the whole animated effect is ~15 lines of CSS transition, not a library. Step "Timer & milestones" spotlights **two** elements at once (their bounding-box union) since it explains both together.
- Auto-scrolls each target into view (`scrollIntoView({block:"center"})`) before measuring, and **blocks user-driven scroll** (wheel/touch/arrow-keys, scoped to allow scrolling inside the wizard panel itself) for the wizard's lifetime — without this, a user scrolling mid-transition would race the auto-scroll and leave the spotlight measuring a stale position.
- Below the `md` breakpoint (768px, same as the rest of the app), spotlight targeting is skipped entirely (the calendar target is `hidden md:block` anyway) and every step falls back to a plain centered dialog.
- **Trigger, once only, per guest-browser or per-account (not per-visit)**: signed-in — `hub_onboarded_at IS NULL`, server-computed, no flash. Guest — absence of a `hub_onboarded_guest` localStorage flag. A returning guest or a user who's already onboarded never sees it again; it's never a forced gate, always skippable (Escape / backdrop click / a persistent "Skip" control all trigger the same skip path, at any step).

### Guest mode — local-only, never touches the DB, migrates on sign-in
Genuinely new territory for this codebase (confirmed via a full-repo grep: no `localStorage`/`sessionStorage` usage existed anywhere before this). Guests can complete the entire onboarding flow and use the whole planner (add/drag/resize items, log study sessions, edit subjects) with **zero Supabase calls** — everything round-trips through `localStorage` (`hub_guest_items`, `hub_guest_study_log`, `hub_guest_subjects`, `hub_onboarded_guest`), using the exact same snake_case row shape (`components/hub/hub-row.ts`'s `hubItemToRow`/`rowToHubItem`) as the DB, so migrating later is a straight upsert with no reshaping.

- A persistent "Guest mode — sign in to save" pill lives in `HubHeader` for the entire unauthenticated session (not just right after onboarding — local data is at ongoing risk, private windows/clearing site data/etc., not just a one-time-onboarding-moment risk), plus a one-time dismissible toast right after onboarding finishes/skips.
- **New `?next=` redirect-back param** — did not exist anywhere in this app before (every login/signup redirect was hardcoded). Wired through `app/login/page.tsx` → `LoginForm` → `signIn`/`signInWithGoogle` (`app/auth/actions.ts`) → `app/auth/callback/route.ts`, guarded by a new `isSafeNext()` helper (`app/lib/isSafeNext.ts`, blocks open-redirect via `//evil.com`-style values). **Deliberately scoped to `signIn`/Google OAuth only, not `signUp`** (which still always lands on `/profile/edit` first, unchanged) — a guest bouncing off Hub's CTA is more likely to already have an account. No-`next` behavior is byte-identical to before on every touched file — this is the piece most likely to get missed if this flow is ever extended, since it touches shared auth code used by every page, not just Hub.
- **Migration** (`hub.tsx`'s own `useEffect`, gated on `mounted && userId`, keyed `hub_migrated_<userId>` so it's per-account not per-browser): fires whenever Hub mounts signed-in with leftover guest data in localStorage — **not** tied to arriving via the `next` redirect specifically, since a guest could just as easily sign in from a different tab and visit `/hub` normally later. Calls `bulkImportHubItems`/`bulkImportStudyLog` (both `.upsert(..., {onConflict, ignoreDuplicates: true})` as a second idempotency layer beyond the localStorage marker), then clears the guest keys and refetches from the server as the new source of truth.

### Recommended Resources — real data, not mock rows
The task details panel's "Recommended Resources" now queries real, subject-matched `resources` rows (`getHubRecommendedResources()` in `app/lib/actions/hub.ts` — one `getResourcesPage({subject, sort:"most_liked", pageSize:3})` call per Hub subject, `Promise.all`'d in `app/hub/page.tsx` and passed down as `Record<SubjectId, Resource[]>`) instead of the three hardcoded fake `MOCK_RESOURCES` rows the prototype pass used. Server-side prefetch, not client-fetch-on-panel-open — keeps `TaskDetailsPanel` a pure lookup with no new loading state, and works for guests too since `getResourcesPage` already has a working logged-out branch.

### Known limitations, not bugs
- No `deleteHubItem` action/UI — wasn't in the original spec either; trivial to add later following the exact same action pattern as the others in `app/lib/actions/hub.ts`.
- **Never visually verified signed-in** — same standing constraint as the rest of this app's auth-gated features, no test account available to Claude across the entire life of this project, still true as of the update below.

### Update, 2026-09-16 (later session): focus mode, calendar sync, .ics import, subject defaults, session picker

Picked up from "Next session: run the three SQL blocks... then do a real signed-in pass" — instead grew into a much bigger session covering three planned phases plus several follow-up requests. In rough order:

- **Full-screen focus mode** (`components/hub/focus-mode.tsx`) — reached from the compact `FocusTimerCard` (a new expand icon, shown once a session is running/paused) or automatically when starting a session from a task's "Start focus session" button. Big serif countdown, linked task name + subject color, Pause/End, a "Session N of 4" counter (ephemeral, per-task, resets on reload — same session-only scope as the timer itself), and a collapsible "Helpful resources" panel whose entries link straight to the resource (both here and in the normal task details panel — they were bookmark-only before). Also best-effort requests real OS-level fullscreen (`document.documentElement.requestFullscreen()`) in the same click that opens focus mode, since that's the only way browsers honor it; falls back to the existing full-viewport overlay look if refused.
  - **Real bug hit and fixed along the way**: this app's `globals.css` redefines the Tailwind spacing scale (`--spacing-xl: 2.5rem`, `--spacing-md: 1rem`, etc.) — meaning any bare Tailwind sizing utility whose key collides with those names (`max-w-xl`, `max-w-md`, ...) silently resolves to the *custom* value instead of Tailwind's real default (36rem/28rem). Bit this session twice — once collapsing focus mode's whole layout to a 40px column, once re-capping the (already being fixed) mobile guest banner down to ~16px wide. **Rule going forward: never use a bare named Tailwind size utility in this codebase without checking it isn't one of xs/sm/md/lg/xl/gutter/margin first — use a bracketed value (`max-w-[28rem]`) instead.**
- **"Today's Focus" strip** (`components/hub/today-focus.tsx`) — sits between the header and the calendar on `/hub`. Today's timed items plus anything still overdue from an earlier day, sorted by time; the earliest unfinished one is highlighted with an inline "Start focus" button; every item has an optimistic done checkbox. Three states: normal list, "You're done for today," or "Nothing scheduled" + an Add item button. Purely derived from the `items` state `/hub` already holds — no new fetching.
- **ICS calendar feed subscription** — `calendar_feeds` table (`user_id` unique, `token_hash`, `include_tasks`/`include_study_blocks`, `created_at`, `last_accessed_at`), a new `GET /api/calendar/[token].ics` route (service-role client scoped to that one file, rate-limited, same-404-either-way on any failure), and a "Sync to calendar" dialog. Token is 32 random bytes, only its SHA-256 hash is ever stored, and the real URL is shown exactly once at generation time. ICS generation itself (`app/lib/ics.ts`) uses the `ical-generator` package (new dependency, approved). Item window: 30 days back to 12 months ahead; university deadlines/IB assessments always included regardless of the include-toggles.
- **`.ics` file import** — "Import" in the Hub header. Parses on the server with `ical.js` (new dependency, approved) in a new pure module (`app/lib/ics-import.ts`, deliberately no Next/Supabase imports so it's unit-testable whenever this repo gets a test runner — it still doesn't have one, so nothing was added unprompted). Handles VTIMEZONE/TZID, recurring-event expansion via `ICAL.Event#getOccurrenceDetails()` (so a single edited occurrence in the source calendar keeps its own overridden title/time), a 500-event cap, and skips any UID matching our own feed's format (`item-...@diplomahub`) so re-importing your own subscribed calendar is a no-op. Preview-before-save with per-row subject/type suggestion (alias-matched against the user's *actual* Hub subject picks, not the full 16 — see `app/lib/ics-import.ts`'s `EXTRA_ALIASES`, which deliberately excludes bare "math"/"maths" as too ambiguous between AA/AI, and has no home for the spec's suggested "Spanish B" alias since this app has never had a Spanish subject). Saved rows get `source: 'ics_import'`, `external_uid`, and an `import_batch_id`; re-importing the same file upserts on `(owner_id, external_uid)` instead of duplicating. Every existing hub-item edit action now clears `import_batch_id` on write, so "Undo import" only ever removes rows the user hasn't touched since.
  - **Real bug hit and fixed**: `icalTimeToDate()` deliberately does its own UTC-safe date math for floating/date-only values instead of trusting `ICAL.Time#toJSDate()`, which silently uses the *server process's own* local timezone for those — verified locally (non-UTC dev machine) that this shifted every all-day imported event back a day. Vercel's Node runtime defaults to UTC so this specific bug likely wouldn't have been visible in production, but the fix doesn't depend on that being true.
  - **Real bug reported by the user, fixed**: `hub_items_owner_external_uid_idx` was created as a *partial* unique index (`where external_uid is not null`). Postgres will only use a partial index for `ON CONFLICT (columns)` inference if the conflict clause repeats the same `WHERE` predicate — which Supabase-js's `.upsert({onConflict: "..."})` has no way to express — so every real import hit "there is no unique or exclusion constraint matching the ON CONFLICT specification." Fix (given to the user, **not confirmed run**): drop and recreate the index without the partial predicate — safe, since Postgres already treats every `NULL` as distinct under a plain unique index, so manual items (`external_uid IS NULL`) never collided in the first place.
    ```sql
    drop index if exists public.hub_items_owner_external_uid_idx;
    create unique index if not exists hub_items_owner_external_uid_idx on public.hub_items (owner_id, external_uid);
    ```
- **New subjects default to core-only, not all 16** — `computeMySubjectIds(null)` (`components/hub/onboarding/subject-cap.ts`) now returns just TOK/EE/CAS/General instead of every subject. Affects any account that hasn't picked yet, including one that explicitly skips the onboarding wizard (the wizard already converts "nothing selected" to `null`). This is the single shared helper the client *and* the `.ics` import subject-matching both use, so it changed both at once.
- **Editable graduating session** — the static "Session: May 2027" text in the Hub header is now `components/hub/session-picker.tsx`, a small click-to-open dropdown (Nov 2026 / May 2027 / Nov 2027 / May 2028 / Nov 2028). Default shown value: guests and anyone without DP1/DP2 on file get "May 2027"; DP2 → "May 2027", DP1 → "May 2028" (`defaultHubSession()` in `components/hub/format.ts`). A manual override persists to `localStorage` (keyed per-account, `hub_session_override:<userId|"guest">`) — **not** written to the `users` row, so it doesn't follow the account across devices/browsers. Flagged to the user as an easy follow-up (one new profile column) if they want real cross-device persistence.
- **Mobile/tablet bugs found from real device testing and fixed**: the compact `FocusTimerCard`'s button row could overlap the countdown text on a narrow column width (e.g. the 3-of-12 grid slot on an iPad-width layout) — now `flex-wrap`s onto its own line instead. The guest-save and import-success banners had no width constraint at all on phone, shrink-wrapping down to almost nothing and wrapping the sentence across many short lines — now a properly wide card (`max-w-[28rem]`, note not `max-w-md` — see the spacing-scale collision note above) on mobile, reverting to the original compact pill from `sm:` up.

**SQL status as of this update** — three pieces from Phase 2/3 of this session, on top of the three from the original 2026-09-16 Hub build (still themselves not explicitly reconfirmed):
1. `calendar_feeds` table + RLS — **status unclear**, never got an explicit "ran it" from the user.
2. `hub_items` new columns (`source`, `external_uid`, `import_batch_id`) + the (now-known-broken) unique index — **columns are confirmed applied** (inferred: the ON CONFLICT error above could only happen if they existed), but the **index fix above is not confirmed run**.
3. If the index fix hasn't been run yet, `.ics` import will still fail with the same ON CONFLICT error — check this first if it's reported again.

## Mobile responsiveness — large pass, not yet visually verified by Claude

The app was built desktop-first with many literal fixed-pixel Tailwind utilities (`px-50`, `w-200`, `h-700px`, etc.) and zero mobile nav. A large sweep fixed this; the pattern used throughout was **mobile-first override, original value preserved at whatever breakpoint the layout was actually designed for** (`sm`/`md`/`lg`/`xl` chosen per-case to match, not a blanket rule) — the intent was to change nothing at laptop/desktop widths.

Real bugs found and fixed along the way (not just "add a breakpoint"):
- **`components/Navbar.tsx`** had nav links as `hidden md:flex` with **no mobile alternative at all** — phone users had zero way to navigate. Added a hamburger menu (`md:hidden` toggle + slide-down panel).
- **`components/Footer.tsx`** had `w-400` (1600px!) on a flex child, in the footer that renders on **every single page** — this alone was enough to cause sitewide horizontal overflow regardless of any other page-level fix. This was the actual root cause of a "margin on the right of the whole site" complaint that persisted through an earlier fix pass.
- **`components/profileMenu.tsx`** (the nav auth dropdown) was clipping the user's display name on narrow screens — not a sizing issue but a classic flexbox gotcha: a flex item's `min-width` defaults to `auto` (i.e., "never shrink below content width"), so the name couldn't wrap and just got squeezed/clipped by the row instead. Fixed with `min-w-0` on the flex column plus `break-words`; also hid the secondary "· Npts"/"Diploma Pro" text below `sm` so the name has room.
- **`components/pills.tsx`** `TitleTag` pills had a fixed 100px width that "IB Educator" doesn't fit in — text visibly spilled past the pill's rounded background ("pills sticking out"). Fixed to size-to-content.
- Viewport meta tag was a stray manual `<meta>` element rendered between `<html>` and `<body>` (not guaranteed to hoist into `<head>` correctly) — replaced with the proper `export const viewport: Viewport = {...}` in `app/layout.tsx`.
- Many two-column `basis-2/3`/`basis-1/3` detail-page layouts (resource/article/discussion detail, profile page, ProfileInfo tabs) now stack below `lg`. Card grids (`grid-cols-3`) now collapse via `sm:`/`lg:`. Various header/action rows across comment cards, discussion cards, and resource-panel footers got `flex-wrap` since they were silently clipping content (not just "too cramped") under `overflow-hidden` card wrappers.
- `body { overflow-x: hidden }` added in `globals.css` as a blanket safety net.

**Caveat for whoever picks this up:** none of this has been visually verified in an actual mobile viewport by Claude — the preview/dev-server tooling attempt was blocked mid-session by a port-3000 conflict with the user's own running `npm run dev` (their tunnel needs that specific port for OAuth). Every fix here is a real, verifiable class-level change, but a manual pass on an actual phone (or the preview tool, started on a *different* port) is worth doing, especially on the write/upload forms and the roadmap page's custom timeline layout (which had its decorative rail simply hidden below `lg` rather than redesigned — the fixed `gap-[300px]` alignment trick it used doesn't translate to a stacked layout).

## Pre-launch hardening pass (2026-07-21)

A full pass through the checklist a launch actually needs: security audit, input/upload validation, DB indexes, real pagination, error boundaries, rate limiting, error tracking. Order matters for anyone re-verifying this — SQL had to be handed to the user for several pieces and may not be confirmed run.

### Security audit findings — all fixed
- **`middleware.ts` was dead code.** It lived at `app/middleware.ts`; Next only loads middleware from the project root (or `src/`). Moved to root — this means Supabase's session-refresh logic (`supabase.auth.getUser()` on every request) **is now actually running for the first time**. If anything about session/auth timing behaves differently post-launch than it seemed to before, this is why — it's a fix, not a regression, but it's a genuinely new code path executing in production. **Since renamed again**: Next.js 16 deprecated the `middleware.ts`/`middleware()` convention in favor of `proxy.ts`/`proxy()` (same file, same `config`/`matcher`, just the filename and exported function name change — see [migration doc](https://nextjs.org/docs/messages/middleware-to-proxy)). The file is now `proxy.ts` at the project root exporting `proxy()`. If you see the file under the old name or the old function name anywhere, that's stale.
- **HTML injection in notification emails** — fixed via `escapeHtml()`, see Notifications section. Only matters once `EMAIL_SENDING_ENABLED` flips to true.
- **No server-side input validation anywhere** — client "required" attributes were the only guard on every create/update action. Added `app/lib/validation.ts` (`requireField`/`optionalField`/`requireOneOf`) and applied it across resources/articles/discussions/replies/comments/feedback/signup/profile-edit. Tag fields (`subject_tag`/`type_tag`/`year_tag`) are now validated against the same option lists the dropdowns use (`components/pills.tsx`'s `SubjectTags`/`ResourceTypeTag`/`YEAR_OPTIONS`, plus a local `DISCUSSION_TYPE_OPTIONS` in `discussions.ts` mirroring `discussion-panel.tsx`'s `typeTags` since that one isn't in `pills.tsx`).
- **No file upload validation** — `app/lib/actions/upload.ts` now validates extension against an allowlist per upload type (resources: pdf/doc/docx/png/jpg/jpeg, 15MB; article covers: png/jpg/jpeg/webp, 5MB; avatars: same images, 3MB) **and explicitly sets the storage `contentType`** from the validated extension rather than trusting the client-supplied `file.type` — so even a spoofed MIME type can't get stored/served as something dangerous (e.g. SVG-with-script under an image extension).
- Confirmed solid, no changes needed: RLS/column-grant pattern, IDOR protection (every update/delete scopes to `author_id = auth.uid()` unless admin), article HTML sanitization (`sanitizeHtml` allowlist + forced `rel=noopener noreferrer`), no service-role key ever reaching a `"use client"` file.
- **Still open, not code**: Google OAuth consent screen still in Testing mode (see Known gaps).

### Real server-side pagination — the big one
All three main listing grids (`/resources`, `/articles`, `/community`) used to fetch **every published row** unbounded on every page load and do filter/sort/pagination entirely client-side with `.slice()` — the page-number buttons only controlled what rendered, not what was fetched. Rewritten to filter/sort/paginate in the query itself:

- **Resources**: `getResourcesPage()` in `resources.ts` — plain `.eq()`/`.order()`/`.range()`, `{count: "exact"}` for total pages. `getResourcesWithUserState()` (old, unbounded) is kept only for the homepage's simple `{limit: 6}` featured-resources call.
- **Articles**: `getArticlesPage()` in `articles.ts`, same shape. The old unbounded `getArticlesWithUserState()` was fully replaced and **deleted** (nothing else called it). Note the UI's "subject" pill filter actually filters the `topic` column, not a `subject_tag` column — `topic` is itself constrained to the same `SubjectTags` option set in the write form, so this isn't as loose as it sounds (`app/articles/page.tsx` used to do this mapping client-side with `subject_tag: a.topic`; now `getArticlesPage` filters `topic` directly).
- **Community/Discussions**: the hard one — "Hot" sort is a computed formula (`like_count + reply_count / (daysOld+2)^1.5`), not a plain column, so it can't be a normal `.order()`. Added two Postgres functions (SQL below, **must be run by the user**): `get_discussions_page(...)` and `count_discussions(...)`, called via `.rpc()` from `getDiscussionsPage()` in `discussions.ts`. Old `getDiscussionsWithUserState()` (unbounded) was deleted. The "Trending This Week" cards (most-active + essential) now come from a separate bounded `getTrendingDiscussions(limit=10)` call (top 10 by hot score, unfiltered) rather than sorting the entire table client-side.
- All three grid components (`resources-grid.tsx`, `article-grid.tsx`, `community.tsx`) now hold only the current page in state, fetch a new page via the corresponding action on any filter/sort/page change (client components calling server actions directly), and skip the redundant duplicate fetch on mount via an `isFirstRender` ref (page 1 was already fetched server-side and passed in as `initialItems`).
- Numbered pagination UI was kept as-is (not switched to "Load more") — that was a deliberate choice, see git history if reconsidering.
- **DB indexes**: added on every column now actually filtered/sorted/joined on — `published`/`subject_tag`/`type_tag`/`year_tag`/`author_id`/`created_at`/`download_count`/`like_count` on `resources`; equivalent on `articles`/`discussions`; every FK column on `likes`/`comments`/`saved_items`/`discussion_replies`. SQL below, **must be run by the user**, uses `if not exists` so it's safe to re-run.

**SQL to run** (discussions pagination RPCs):
```sql
create or replace function get_discussions_page(
  p_subject text default null, p_type text default null, p_year text default null,
  p_sort text default 'newest', p_limit int default 6, p_offset int default 0
)
returns setof discussions language sql stable as $$
  select * from discussions
  where (p_subject is null or subject_tag = p_subject)
    and (p_type is null or type_tag = p_type)
    and (p_year is null or year_tag = p_year)
  order by
    case when p_sort = 'hot' then
      like_count + reply_count / power(extract(epoch from (now() - created_at)) / 86400 + 2, 1.5)
    end desc nulls last,
    created_at desc
  limit p_limit offset p_offset;
$$;

create or replace function count_discussions(
  p_subject text default null, p_type text default null, p_year text default null
)
returns bigint language sql stable as $$
  select count(*) from discussions
  where (p_subject is null or subject_tag = p_subject)
    and (p_type is null or type_tag = p_type)
    and (p_year is null or year_tag = p_year);
$$;

grant execute on function get_discussions_page to authenticated, anon;
grant execute on function count_discussions to authenticated, anon;
```
(Index SQL is long — see git history of this file's commit for the full script, or regenerate: every FK/filter/sort column across `resources`/`articles`/`discussions`/`likes`/`comments`/`saved_items`/`discussion_replies`.)

### Error boundaries — new
`app/error.tsx` (route-level, keeps Navbar/Footer, styled to match the design system), `app/global-error.tsx` (only fires if the root layout itself throws — has to render its own `<html>/<body>` since it replaces everything), `app/not-found.tsx` (styled 404, there wasn't one before — Next was falling back to its generic default page). All three report to Sentry when configured.

### Rate limiting — configured and live as of 2026-07-23
`app/lib/ratelimit.ts` — Upstash Redis + `@upstash/ratelimit`, sliding window. **`UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are filled in and confirmed live** — no longer a no-op. Four buckets: `auth` (5/min, keyed by IP via `x-forwarded-for` — signin/signup/password-reset request), `write` (10/min, keyed by user id — comments, discussions, replies, feedback), `download` (30/min, keyed by user id — `downloadResource`), `toggle` (60/min, keyed by user id — like/save toggles; higher limit since legitimately clicked a lot while browsing a feed, added 2026-07-23 after the initial pass left these two uncovered).

### Security headers — new (2026-07-23)
`next.config.ts` `headers()` — CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/mic/geolocation all denied, unused). **Deliberately not nonce-based** — Next's nonce-based CSP pattern requires forcing every page into dynamic rendering (kills static optimization/ISR sitewide, real cost for a content-heavy app like this one), so this uses the documented "without nonces" fixed-header approach instead, with `'unsafe-inline'` on `script-src`/`style-src` (needed because `likeButton.tsx`/`saveButton.tsx` set liked/saved color via inline `style`, and Next injects inline hydration scripts). `connect-src` allowlists Supabase + Sentry ingest domains. Verified via `curl -I` that headers actually apply, and via browser console that nothing gets blocked.

### CI pipeline — new (2026-07-23)
`.github/workflows/ci.yml` — runs `npm run typecheck` (new script, `tsc --noEmit`) and `npm run lint` on every push/PR. This is CI only, not CD — Vercel already auto-deploys independently of whether this workflow passes; they aren't wired together (Vercel's Git Integration has a "require status checks" setting that would connect them, not currently enabled). Had to fully clean the 38-problem lint baseline first (see Known gaps) so this pipeline wasn't red from its first run.

### Error tracking — new, no-ops until configured
`@sentry/nextjs`, wired the current (Next 15+/16) way: `instrumentation.ts` (root, server + edge init, `onRequestError` hook) and `instrumentation-client.ts` (root, client init) — **not** the older `sentry.client.config.ts` convention. `Sentry.captureException()` added to both error boundaries. No source-map upload configured (`withSentryConfig` wrapper skipped — needs a Sentry auth token + org/project slugs the user hasn't provided yet); stack traces will be minified until that's added via `npx @sentry/wizard` later. **Needs `NEXT_PUBLIC_SENTRY_DSN`** in `.env.local` (user needs to create a free Sentry project first) — no-ops entirely until set.

### Rollback (Vercel) — process, no code needed
If a deploy goes bad in production: **Vercel dashboard → project → Deployments tab → find the last known-good deployment → "..." menu → Promote to Production.** This re-points production traffic at that build instantly (no rebuild, no redeploy wait) — it's the fastest path back to a working state, faster than `git revert` + waiting on a new build. Equivalent via CLI: `vercel rollback [deployment-url]`. Note this only rolls back the **app code** — it does not undo any Supabase schema/SQL changes that shipped alongside it, since those are applied by hand in the SQL editor and aren't versioned with deploys. If a bad release included a schema change, rolling back the app without also reverting the SQL can leave the (old) code talking to a (new) schema shape — check what changed in Supabase before assuming a Vercel rollback alone fixes things.

### Explicitly deferred, not done
- **Load/stress testing** — premature without real production traffic patterns; revisit if a specific high-traffic event is expected.
- **Caching beyond Next's defaults** — no evidence yet that Supabase read load needs it.
- **Comment/reply pagination** on detail pages — still fetches everything for one resource/article/discussion at a time; lower risk than the three main grids since it's bounded by one item's engagement, not sitewide growth.

## Feature status by area

### Resources
- Routes: `/resources`, `/resources/[slug]` (folder says `[slug]`, value is actually the `id` — harmless), `/resources/upload`, `/resources/[slug]/edit`.
- Upload: **a file, a link, or both** (2026-09 session, replaced the old file-XOR-link `"External Link"` type model — see Data model). `DownloadButton` takes `kind: "file" | "link"` and can render twice on one resource card/detail page if both are attached.
- Mandatory: title, subject, type, year.
- **"View Full Profile" button on the detail page was completely dead** (a plain `<button>`, no `href`/`onClick` at all) until fixed 2026-07-23 — now correctly `<Link href={\`/profile/${resource.author_id}\`}>`. The equivalent button on the discussion detail page was already correct; worth double-checking any *other* copy-pasted detail-page buttons if something similar turns up again, since these three detail pages (resources/articles/community) share a lot of copy-pasted structure and this kind of drift is easy to miss.
- **Logged-out download attempts show a toast, not a silent failure** (2026-09 session) — `DownloadButton` shows "Log in to access every single resource for FREE" as a small pill, portaled to `document.body` via `createPortal` (not positioned relative to the button) so it can't be clipped by a resource card's `overflow-hidden` — an earlier version that positioned it absolutely relative to the button *was* clipped on the grid pages, caught via user report + screenshot.
- **Safari-specific popup-blocker bug, fixed (2026-09 session)**: the "Visit Link" flow called `window.open(url, "_blank")` *after* `await downloadResource(...)` — Safari (much stricter than Chrome/Firefox here) only treats `window.open()` as a trusted user-initiated action if it's called synchronously inside the click handler, with no `await` in between. Fixed by opening a blank tab immediately on click (before the await), then setting `newTab.location.href` once the real URL is known — with a same-tab-navigation fallback if even the synchronous open gets blocked (e.g. popups disabled site-wide).
- **Resource card title/description overlap bug, fixed (2026-09 session) — a real CSS lesson worth remembering**: the title `<h3>` had a fixed `h-20` height with no `line-clamp`, so a 3-line title would visually bleed past its box into the description below (the box doesn't grow, but unclamped text isn't clipped either). Adding `line-clamp-2` fixed the clipping, but **combining it with any explicit `height`/`min-height` reintroduced the same bug in a subtler form** — line-clamp's own overflow-hidden clips at its box's outer edge, and any height/min-height taller than the natural 2-line content height gives the `-webkit-box` model room to lay out a *partial* 3rd line before that edge, producing "ellipsis on line 2 + a sliver of line 3 bleeding through." The final fix has `line-clamp-2` with **zero** height/min-height classes, matching the sibling description paragraph's already-working pattern exactly. **Don't combine `line-clamp-N` with a fixed/min height on the same element** — pick one.

### Articles
- Routes: `/articles`, `/articles/[slug]`, `/articles/write`, `/articles/[slug]/edit`.
- Tiptap HTML content, sanitized server-side (allowlist: `p, div, br, b, strong, i, em, ul, ol, li, a, blockquote, h2, h3` — never loosen without checking what the toolbar can produce). Cover image upload. Read time computed (`app/lib/readTime.ts`), not stored. Save Draft vs Publish.
- Mandatory: title, topic, content.

### Discussions / Community — retired, 2026-09-16 (later session), replaced by the Hub
`/community` and its whole UI (routes, `components/community/`) were **deleted** as part of the homepage redesign below — the Hub (`/hub`) is now where logged-in personal planning/engagement happens instead. `/community`, `/community/[id]`, `/community/write`, `/community/[id]/edit` all now 308-redirect to `/hub` via a wildcard rule in `next.config.ts`'s new `redirects()`. **The `discussions`/`discussion_replies` DB tables were deliberately left untouched** — nothing about the data layer changed, only the UI that rendered it. `app/lib/actions/discussions.ts` still has its full CRUD/admin-facing exports (`/admin`'s Discussions tab still works, on purpose — admins can still moderate/delete old content); only `getDiscussions()` (used solely by the old homepage) was removed as genuinely dead code.

### Profile (`/profile/[userId]`)
- Tabs: Resources, Articles, Comments, Saves (own profile only), Drafts (own profile only) — the Discussions tab, "Discussions Started" stat, and Saved Discussions were removed in the same pass as the section above. "Load more" pattern (`LoadMoreList.tsx`), not numbered pagination — intentional, distinct from the main grids.
- Own-profile edit page (`/profile/edit`) now supports **avatar upload** (click the circle, immediate upload via `uploadAvatar()`) and the "Save changes" button redirects to the profile page instead of an in-place "Saved!" message.
- Drafts link to the article's edit page (unpublished detail pages 404).
- **`/profile/edit` had a real, currently-shipped-broken bug**: the page wrapper was `px-[600px]` (600px of padding *per side*, no responsive override) instead of the almost-certainly-intended `max-w-[600px] mx-auto` — left negative/zero content width on anything under ~1300px wide, i.e. broken for most phones/tablets/laptops. Missed by the earlier mobile-responsiveness pass since this page wasn't in its file list. Fixed 2026-07-23.

### Homepage (`/`) — redesigned, 2026-09-16 (later session), Hub-first

**Fully rebuilt, uncommitted as of this update — see "Homepage redesign" section below for the complete breakdown.** The trending-discussions and "Top Contributors" sections described in earlier session notes **no longer exist** — the homepage now branches on signed-in state (`getCurrentUserId()`) between a Hub-focused marketing page (signed out) and a personal dashboard (signed in). Featured Resources' underlying logic (`getFeaturedResources()` in `resources.ts`, the `get_featured_resources` RPC + `DISTINCT ON`-per-subject-tag fallback) is unchanged and still reused on both variants — its RPC-confirmation status is exactly as uncertain as it always was, nothing new learned about it this session.

## Homepage redesign — Hub-first, discussions removed (2026-09-16, later session)

**Entered via plan mode at the user's request** (they asked to see the plan before any editing) — the approved plan is what shipped, essentially unchanged except for two real bugs caught during verification (below). **Everything in this section is uncommitted** — still sitting in the `thehub` working tree alongside a manual, unrelated rewrite of `app/legal/privacy-policy/page.tsx` the user made directly (not Claude — see "Environment quirks" below).

**Why**: `/community` (discussions) is being fully replaced by the Hub as the app's logged-in-engagement surface. Discussion UI needed to come out of the whole site (not just the homepage), with old URLs redirecting instead of 404ing, and the homepage itself needed to become the front door for the Hub/resources product instead of the old community-platform pitch.

**Removed sitewide** (DB tables **not** touched, see the "Discussions / Community" note above):
- Deleted `app/community/**`, `components/community/**`, `components/home/{trending,trending-item,top-contributors}.tsx`, and the now-dead `getDiscussions()` export.
- `next.config.ts` gained a `redirects()` block (this app had none before): `/community` and `/community/:path*` → `/hub`, both 308 permanent.
- `app/sitemap.ts`: dropped the `discussions` table query and every `/community` entry it fed; added a `/hub` entry.
- `ProfileInfo.tsx`: removed the Discussions tab, "Discussions Started" stat, and Saved Discussions list (Saved Resources/Articles untouched).
- `/leaderboard` copy: dropped "and discussions" from both the metadata description and the on-page paragraph.

**Signed-out homepage** (`components/home/signed-out-home.tsx` + new `hero.tsx`/`how-it-works.tsx`/`resources-by-subject.tsx`/`final-cta.tsx`): hero with the exact requested headline/copy/buttons, a static 3-step "how it works," real per-subject resource counts (`Object.keys(SubjectTags)` from `pills.tsx` × `getResourcesPage({subject, pageSize:1}).totalCount` in parallel, same fan-out shape as `getHubRecommendedResources`) each linking to `/resources?subject=<name>`, the existing Featured Resources logic reused as-is, then a final CTA.
- **The hero's Hub screenshot doesn't exist yet** (`public/images/hub-preview.png` — user said they'd add it later). `hero.tsx` checks `fs.existsSync(...)` server-side at request time and renders a plain styled placeholder card when it's missing, so dropping the real file in needs **zero code changes** to start working.
- **`/resources` never supported a `?subject=` deep link before** — `ResourceGrid`'s subject filter was pure local `useState`, never read the URL. Small additive fix: `app/resources/page.tsx` now reads `searchParams.subject` server-side (same pattern as the homepage's existing `error_code` handling) and passes it both into the initial query and down as `ResourceGrid`'s new optional `initialSubject` prop.

**Signed-in dashboard** (`components/home/signed-in-home.tsx` + `dashboard-greeting.tsx`/`dashboard-hub-summary.tsx`/`dashboard-resources.tsx`/`dashboard-setup-prompt.tsx`): greeting (time-of-day `getGreeting()` extracted out of `hub-header.tsx` into shared `components/hub/format.ts` so both places import one copy) + "Open the Hub" button. If `hub_onboarded_at` is null, that's **all** that renders, plus one "Pick your subjects" prompt card — no empty dashboard. Otherwise: a live Today's Focus + next-3-deadlines summary (`dashboard-hub-summary.tsx`, a `"use client"` wrapper around the existing `TodayFocus`/`UpcomingDeadlinesCard` components — both already just take `items`+callbacks, no Hub reducer/timer coupling to fight; the done checkbox is genuinely real, optimistic local state calling the actual `updateHubItemStatus` action), "New in your subjects" resources (keyed off the profile's **raw** `hub_subjects` picks specifically, not the core-padded `computeMySubjectIds()` set, since TOK/EE/CAS/General are default-included for everyone and using the padded set would show identical "personalized" resources to every account — falls back to plain recent resources if `hub_subjects` is null/empty), and Featured Resources again.
- `UpcomingDeadlinesCard` gained a small additive `limit` prop (default 6, unchanged for `/hub`'s own use; `3` on the dashboard).
- `hub_subjects` stores Hub's own `SubjectId` values (`"math_aa"`), not the real `subject_tag` string (`"Math AA"`) resources are queried by — `dashboard-resources.tsx` bridges the two via `getSubject()` from `components/hub/mock-data.ts`, the same helper `getHubRecommendedResources` already relies on for exactly this reason.

**Two real bugs caught during verification, not just assumed correct:**
1. The final CTA's "Start planning free" button was invisible — reused the shared `Button` component (hardcoded `bg-primary text-on-primary`) on a section that's itself `bg-primary`. Fixed with a plain inverted-color link instead of trying to override `Button` via `className` (per the Tailwind-class-order gotcha noted elsewhere in this doc, appending an override class in the string doesn't reliably win the cascade).
2. Reusing `ResourceHome` (`components/home/article-section/article-home.tsx`) verbatim for both the real Featured Resources section *and* the new personalized "New in your subjects" section would have shown two identical "Featured Resources / Handpicked by top contributors" headings, with the personalized one mislabeled. Gave it optional `title`/`subtitle` props (defaults preserve the exact original text for every existing caller).

**Verified live** (signed-out only — no test account, same standing constraint as everything else auth-gated): hero/sections/real subject counts render, all three `/community*` URL shapes redirect to `/hub`, `/resources?subject=Physics` lands correctly pre-filtered, leaderboard copy updated, a profile page shows zero discussion traces, `/sitemap.xml` is clean of `/community`. **Never tested signed-in**: the dashboard itself, the "not onboarded" prompt card, and the personalized-resources fallback all need a real signed-in pass before this is safe to consider done.

## Established code patterns

- Server actions live in `app/lib/actions/{resources,articles,discussions,upload,profile,saved-items,likes,comments,admin,feedback,notifications}.ts`, all `"use server"`, all return `ActionResult<T> = {success:true,data:T} | {success:false,error:string}`.
- Create/edit forms are client components. `SortDropdown`/`FilterDropdown` are plain `<button>`-based, not native inputs — value goes in via `formData.set(...)` in the submit handler, validated manually, internal buttons need `type="button"`.
- Edit routes reuse the create form component with an optional prefill prop.
- Delete buttons: `confirm()` guard → call action → `router.push()`/`router.refresh()` on success.
- Card/panel components accept an optional `href` prop to override the default detail-page link (e.g. Drafts tab → edit page instead of the 404-prone detail page).
- `next/image` with `fill` needs an explicit `sizes` prop.
- `next.config.ts`: `experimental.serverActions.bodySizeLimit: "20mb"`, `allowedOrigins` for tunneling (see Auth section). **Changing `next.config.ts` requires a dev server restart.**
- **Server-side validation**: `app/lib/validation.ts` (`requireField`/`optionalField`/`requireOneOf`) — every create/update action validates formData server-side now, not just via client "required" attributes. Use this rather than ad-hoc checks for any new form field.
- **Rate limiting**: `checkRateLimit(kind, identifier)` from `app/lib/ratelimit.ts` — call after the auth check, before doing the write, in any new action that creates content or is otherwise abusable. No-ops until Upstash env vars are set.
- **List pagination**: any new listing page should follow the `get<Thing>Page(filters)` pattern (see Resources/Articles/Discussions, and now the admin Users/Content/Feedback tables too) — filter/sort/paginate in the query via `.range()` + `{count: "exact"}`, return `{items, totalCount}`, never fetch-all-then-slice client-side. If the sort needs a computed/derived score (like Discussions' "Hot", or the homepage's "Featured Resources"), it needs a Postgres function, not a plain `.order()`.
- **Debounce listing-page fetches, don't try to cancel them (2026-07-27)**: every client-side listing component that fetches a new page on filter/sort/pagination change (`resources-grid.tsx`, `article-grid.tsx`, `community.tsx`, and now the admin tables) wraps the fetch in a `setTimeout(..., 300)` inside the `useEffect`, clearing the previous timeout in the cleanup — a real production bug (`TypeError: Failed to fetch`, verified via a controlled browser test: 5 rapid clicks → 1 request after the fix, vs. 5 before) came from rapid clicks each firing their own concurrent Server Action POST with nothing stopping them piling up under Vercel's concurrency limits. **`AbortController` cannot fix this** — verified directly against this Next 16 version's client runtime (`app-call-server.js` → `server-action-reducer.js`): calling an imported `"use server"` function directly (not via `<form action>`) goes through Next's internal `fetch()` with no `signal` parameter exposed anywhere, and there's no public API to cancel an in-flight action call. If a future listing page has this same shape, debounce the trigger — don't reach for AbortController, it doesn't exist here.
- **`<form action={fn}>` with a plain client function can silently eat your loading state (2026-07-27)**: React 19 treats a plain function passed to a form's `action` prop as a transition, and transitions are allowed to skip painting intermediate state entirely if the whole thing resolves quickly enough (an anti-flicker heuristic) — so `setStatus("saving")` etc. might never actually paint before the next state update lands, and a spinner/status text that's provably correct in code review can still never visibly appear. Bit literally every form in the app that used this pattern (login, signup, forgot/reset password, profile edit, resource upload/edit, write article, write discussion) — all fixed by switching to a plain `onSubmit` handler with `e.preventDefault()` and building the `FormData` manually (`new FormData(e.currentTarget)`, or `new FormData(e.currentTarget, submitter)` for a form with multiple named submit buttons like WriteArticleForm's Save Draft/Publish, to preserve which one was clicked). **Any new form needing a visible pending/loading state must use `onSubmit`, not `action={aPlainFunction}`** — the latter is fine for zero-feedback fire-and-forget cases, but not when you need the UI to reliably reflect "in progress."
- **New pages need their own `metadata`/`generateMetadata`** (title, description, `alternates.canonical`) — the root layout only provides defaults/fallbacks now, it doesn't mean every page should skip setting its own. Dynamic detail pages use `generateMetadata()` with the same fetch the page component uses (accepted as two DB calls per request rather than risk wrapping a `"use server"`-file export in React's `cache()`, which may not satisfy Next's Server Actions compiler — see the `SEO` section for why that was tried and reverted).
- **JSON-LD**: use `components/JsonLd.tsx` (`<JsonLd data={...} />`), never hand-roll a `<script type="application/ld+json">` — it handles the `<` escaping needed since this data often includes user-submitted content.
- **Default to `getCurrentUserId()`, not `getCurrentUser()`, for any new "is someone logged in" check (2026-09 session)** — the former reads a free request header `proxy.ts` already sets, the latter does a real Supabase network call. Reach for `getCurrentUser()` only if you need an actual Auth field beyond the id (e.g. `email_confirmed_at`). See the Auth section.
- **A Postgres RPC function's parameter list can't be changed with a plain `CREATE OR REPLACE FUNCTION` (2026-09 session, bit us in production)** — Postgres only replaces a function if the parameter *types* match exactly; adding/removing a parameter creates a second overloaded function alongside the old one, and PostgREST can then fail to pick a candidate for callers using the old signature (`"Could not choose the best candidate function"`). If a table function ever needs a new parameter, `DROP FUNCTION <name>(<old signature>)` explicitly once the new one exists, and `NOTIFY pgrst, 'reload schema'` after. See "Search" section for the real incident.
- **Toasts/popups triggered from inside a card need to be portaled to `document.body`, not positioned relative to their trigger (2026-09 session)** — several resource/article card wrappers use `overflow-hidden` (for rounded corners), which silently clips anything absolutely-positioned that pokes past the card's edge. `createPortal(..., document.body)` + `position: fixed` sidesteps this entirely, at the cost of losing the "anchored right above this specific button" placement (acceptable for a short-lived toast). See the `DownloadButton` login-prompt and `resources-grid.tsx`'s EE-exemplar disclaimer for the pattern.
- **`window.open()` after an `await` gets blocked by Safari specifically** — Safari only trusts `window.open()` as user-initiated if it's called synchronously inside the click handler. If a click handler needs to `await` a server action before knowing the URL to open, call `window.open("", "_blank")` *before* the await to grab a trusted tab handle, then set `.location.href` on it once the URL is known. Chrome/Firefox don't need this; don't skip it just because it works fine in whichever browser you're testing in.
- **`line-clamp-N` and an explicit `height`/`min-height` on the same element fight each other** — combining them can let a partial extra line bleed past the clamp boundary instead of cleanly truncating. Use `line-clamp-N` alone; if you need consistent row heights across a grid of cards with varying title lengths, that's a tradeoff to accept, not something to force with a height class.
- **Chart colors are not the same job as UI colors** — the app's `--color-primary`/`-secondary`/`-tertiary` brand hues fail categorical-chart-safety checks (too dark / too low-chroma to distinguish as chart series, confirmed with the `dataviz` skill's validator). Any new chart needs its own validated palette (see "Admin analytics dashboard" section for DiplomaHub's specific validated triplet) — don't just reuse the brand CSS variables for chart marks.

## Known gaps / pre-existing issues, still open

- ~~`<h1>` is used almost everywhere, not just for page headings~~ — **resolved 2026-07-23** (a later same-day session than the one that scoped this). Was 294 occurrences across 46 files, confirmed zero `<h2>`/`<h3>` anywhere. Fixed in phases: (1) extracted shared `Logo`/`DiplomaProBadge`/`Breadcrumb` components (`components/Logo.tsx`, `components/DiplomaProBadge.tsx`, `components/Breadcrumb.tsx`) covering the wordmark split, the "Diploma Pro" badge (previously an `<h1>` that rendered even when empty), and the "Resources > edit" style nav trails; (2) every grid/list card title (resources/articles/discussions, trending) → `<h3>`; (3) real per-page `<h1>`→`<h2>`→`<h3>` hierarchy on every page, including the 3 legal pages, About, Community, the three write/upload forms, and all remaining stragglers (admin tables, notification dropdown, 404/error pages, etc). Down to 25 occurrences — verified every one is exactly one real `<h1>` per route (either on the page itself or delegated to a child form/table component, e.g. `LoginForm`'s "Log in"). Caught one genuine pre-existing gap along the way: the signup page's only `<h1>` used to be the "Hub" half of the logo wordmark (a degenerate case that predated this fix) — now has a proper "Sign up" heading like every other auth form. Pure tag-semantics swap, no visual change (Tailwind classes already fully controlled appearance) — verified in browser at every phase, including a pixel-check on the homepage hero where two adjacent `<h1>`s got merged into one. See git log (`209a1f1` through `42726f8` on `features`) for the full commit-by-commit breakdown if anything needs re-deriving.
- Lint baseline was fully cleaned in the 2026-07-23 session (was 38 problems / ~15 files as of 2026-07-21, now 0 — `tsc --noEmit` and `eslint .` both exit clean project-wide). If either shows failures again, it's a real regression, not baseline noise — the "pre-existing, ignore it" excuse no longer applies. Two real bugs turned up doing this cleanup, not just cosmetic suppression: (1) `components/data.ts` — 557 lines of pre-database seed content (hardcoded `SEED_RESOURCES`/`SEED_DISCUSSIONS`/`SEED_ARTICLES` etc. from before the Supabase migration) had **zero remaining imports anywhere** — deleted entirely rather than trimmed. (2) `LikeButton`/`SaveButton`/`ShareButton` all accepted a `size` prop that several call sites actually passed a custom value to, but the components silently ignored it and always rendered a hardcoded size — now actually wired through (`size = 36`/`30`/`30` as defaults). Initial pass wrongly assumed `size` was unused (a single-line grep for `size=` missed multi-line JSX call sites) and deleted the prop instead of fixing it — caught immediately by the resulting `tsc` errors across 5 detail-page files, reverted, fixed properly.
- **2026-07-21 hardening pass is confirmed live**: user ran both SQL scripts, filled in Upstash + Sentry + Resend env vars, deployed to Vercel at **diplomahub.org** (canonical/serving domain is `www.diplomahub.org` — the apex 308-redirects to it, see Deployment section below). The `EMAIL_SENDING_ENABLED = false` flag in `notifications.ts` is the one deliberate exception — **its hold date (2026-09-02) has now passed** (today is 2026-09-15) without being revisited (the 2026-09 session was entirely focused on other things). Ask the user whether Resend domain verification actually cleared before flipping this flag — don't assume the passing of the date alone means it's safe.
- **Google OAuth consent screen verification is done** — user confirmed 2026-07-23. See "Deployment & Google OAuth verification" below for the full multi-round debugging story (kept for reference — the same root cause, an incomplete Supabase Redirect URLs allowlist, bit us twice).
- **Google Search Console SEO steps still open** (separate from the OAuth domain-verification, which is done): submit `sitemap.xml`, request indexing on key pages via URL Inspection. No code needed, dashboard only — see "SEO" section below for the full checklist.
- **Three SQL scripts from the 2026-07-27 session are unconfirmed as run** (no migration files in this repo, everything goes through the Supabase SQL editor by hand — see each item's own section for the full SQL and context): (1) the account-deletion `ON DELETE CASCADE`/`SET NULL` foreign keys (Admin panel section) — deletion will fail with a FK-violation error on any user with existing content if this hasn't run; (2) the `calculate_community_trust` floor fix (Community trust score formula section) — likely applied given observed behavior matches, but not explicit; (3) the `get_featured_resources` RPC (Homepage section) — safe either way since `getFeaturedResources()` falls back to plain newest-first if the RPC doesn't exist, just means Featured Resources isn't actually hot-ranked/deduped-by-subject yet if unrun.
- **Community trust score tuning is an open question, not a bug** — see "Community trust score formula" above. Whether `confidence_k = 20` and the log caps make the score too slow to respond to real engagement was raised and left unresolved; revisit if it comes up again.
- **Two more SQL scripts from the 2026-09 session are unconfirmed as run** (same "no migration files, everything by hand" caveat as always): (1) `users.last_active_at` column + self-update grant (see "Retention tracking") — degrades silently to a useless "Active Users" stat if unrun, no error; (2) the `type_tag`/`subject_tag` reshuffle for EE/TOK exemplars (see Data model) — very likely run given later requests depended on its result, but not explicitly confirmed.
- **Donation/payment feature — discussed at length, nothing built or decided.** The user asked about implications of adding a "support us" donation option, and separately how it interacts with a future *paid* Diploma Pro tier (currently free/points-earned). Recommendation given: start with a third-party page (Ko-fi/Buy Me a Coffee) rather than direct Stripe integration, since there's no registered business entity yet — but if paid-Pro is coming soon, doing real Stripe setup once now for both would be more efficient than running two payment providers. Also flagged as a separate, later concern: the day Diploma Pro becomes paid, existing users who earned it for free will need a grandfathering story — silently paywalling something already given away for free would burn trust fast. **No action taken on any of this** — purely a discussion to pick back up whenever the user is ready to decide.
- **No real "downloads over time" trend is possible without a schema change** — `resources.download_count` is a running total with no timestamped per-download log, so the admin analytics dashboard can only chart content-*creation* trends (which have `created_at`), not engagement trends. Would need a new event-log table if ever wanted; treated as a future decision, not built speculatively.
- **Vercel Fluid CPU fixes from the 2026-09 session are unverified for actual impact** — see "Vercel Fluid CPU cost investigation" above. All three code fixes (header-matcher scoping, notification-poll pausing, auth double-validation removal) are reasoned and tested in isolation, but none could be confirmed to have moved the Vercel dashboard's real usage numbers within this session (the charts only reflect full days before the fixes shipped). Check a subsequent full day's numbers before declaring victory.
- **Hub planner's original three SQL pieces (2026-09-16 session) are unconfirmed as run** — `hub_items`, `hub_study_log`, and the `users.hub_subjects`/`hub_onboarded_at` columns+grant. See "Hub planner" section above for the full SQL. Until run, signed-in `/hub` persistence silently fails to save (guest/localStorage mode is entirely unaffected and already fully working).
- **Calendar feed + import SQL/fixes from the 2026-09-16 later session are also unconfirmed/incomplete** — see that section's "SQL status" block for the full detail: `calendar_feeds` table status unclear; `hub_items`'s `source`/`external_uid`/`import_batch_id` columns are confirmed applied, but the unique index on them was created *partial* and needs the drop-and-recreate fix given there (not yet confirmed run) — until it is, every `.ics` import fails with a real "no unique or exclusion constraint matching ON CONFLICT" error, which is exactly what got reported this session.
- **The homepage redesign (2026-09-16 later session) is entirely uncommitted** — everything in "Homepage redesign" above is sitting in the `thehub` working tree, not on `features`. Commit/push it before assuming it's live anywhere. `public/images/hub-preview.png` also still doesn't exist (hero renders a placeholder until it's added — no code change needed when it is).
- **Start the next session here**: run the SQL above (all of it — original Hub tables + the index fix), commit/push the homepage redesign, then do a real signed-in pass — the one thing no session so far has been able to verify, no test account has ever been available to Claude for the life of this project. That pass should cover both the Hub itself (still open from the original 2026-09-16 entry) and everything new since: focus mode, calendar sync, `.ics` import, and the whole signed-in dashboard on the homepage.

## Deployment & Google OAuth verification

Live on Vercel. **Canonical domain is `https://www.diplomahub.org`** — the bare apex `https://diplomahub.org` returns an HTTP 308 redirect to the `www` version (standard Vercel behavior when both domain variants are added with one set primary). This matters a lot for anything that checks URLs without following redirects (see below) — **always use the `www` URL** when configuring external services (Google Cloud Console, Search Console, etc.), never the apex.

Google OAuth verification (moving the consent screen out of Testing mode) hit two errors:
1. *"Home page URL is not registered to you"* — needs domain ownership verification in **Google Search Console**, using a **Domain property** (DNS TXT record), not a URL-prefix property, so it covers both `diplomahub.org` and `www.diplomahub.org` under one verification. (Done as of 2026-07-23 — see below.)
2. *"Privacy policy URL is unresponsive"* — root cause was exactly the redirect above: the consent screen had the apex URL configured, Google's automated checker doesn't follow redirects, so it saw a 308 instead of a 200 and called it unresponsive. Confirmed via direct `curl -I` testing (including with a spoofed Googlebot user-agent) that the `www` URL always served a clean 200.

**Fix applied**: `/legal/privacy-policy.pdf` and `/legal/terms-of-service.pdf` (static files in `public/legal/`, still present, no longer linked from the Footer) were converted to real Next.js pages — `app/legal/privacy-policy/page.tsx` and `app/legal/terms-of-service/page.tsx`, styled like the pre-existing `app/legal/impressum/page.tsx`. Content was transcribed faithfully from the PDFs (read via the PDF tool, not rewritten/summarized) — same sections, same legal wording, same "Last updated: 07/07/2026" date. `components/Footer.tsx` now links to `/legal/privacy-policy` and `/legal/terms-of-service` instead of the `.pdf` paths. This was **both** a UX improvement and a direct fix for the Google verification error — a real HTML page can't 308-redirect the way the apex-domain PDF link did.

**Resolved 2026-07-23** — user confirmed Google OAuth consent screen verification is done (domain ownership via Search Console Domain property + the HTML-page fix above cleared both errors).

### The same root cause bit us twice — Supabase Redirect URLs allowlist

After forgot-password shipped (see below), a real reset-link click landed on `https://www.diplomahub.org/?code=...` — the **bare homepage**, not `/auth/reset-password`. Root cause: Supabase silently ignores any `redirectTo` that isn't in its **Redirect URLs allowlist** (Authentication → URL Configuration) and falls back to the plain **Site URL** instead — exactly the same failure mode that broke Google OAuth login earlier in the project's history (that time it was `/auth/callback` missing from the allowlist; this time `/auth/reset-password` was missing, because it didn't exist yet when the allowlist was last configured). The stranded code then goes stale/single-use, which is why a *second* click attempt shows `?error_code=otp_expired` instead.

**Fix (dashboard, not code)**: add both to Supabase's Redirect URLs — `https://www.diplomahub.org/auth/reset-password` and `http://localhost:3000/auth/reset-password` for local dev. Given this exact bug class has now recurred once, a wildcard (`https://www.diplomahub.org/auth/**` + `http://localhost:3000/auth/**`) is worth doing instead of exact-path entries, so any *future* route added under `/auth/` doesn't need another manual dashboard trip. **Not confirmed whether the user actually applied this fix** — if a fresh password-reset link still dead-ends on the homepage, check this allowlist first before assuming a code bug.

As a separate, permanent UX improvement (kept regardless of the allowlist fix): `app/page.tsx` now detects `?error_code=otp_expired` in `searchParams` and shows a banner ("that link expired or was already used...") instead of silently rendering the normal homepage with a broken-looking URL — covers both a genuinely expired link and this allowlist misconfiguration.

## SEO (2026-07-23)

**`app/sitemap.ts` and `app/robots.ts` are correct as `.ts` files — not a bug.** These are Next.js file conventions that *compile* to real `/sitemap.xml` and `/robots.txt` HTTP responses at request time; verified live via `curl` that both serve proper XML/plaintext with correct headers. The `.ts` extension is how you write the generator code, not what gets served. `sitemap.ts` pulls published resources/articles/discussions from Supabase dynamically; `robots.ts` disallows `/admin`, `/profile/edit`, `/login`, `/signup`, `/forgot-password`, `/reset-password`, `/auth`.

**Per-page metadata** — before this pass, every single page (37 files) inherited the same static global title/description from the root layout, meaning Google would've shown an identical generic snippet for every resource, article, and discussion. Fixed:
- Root layout (`app/layout.tsx`): `metadataBase` (`https://www.diplomahub.org` — the `www` canonical, not the apex), a `title.template` (`"%s | DiplomaHub"`) so child pages just set a short title, Open Graph + Twitter defaults. **Deliberately did not pin an explicit `description` inside `openGraph`/`twitter`** — doing so overrides Next's automatic per-page inheritance sitewide, which was a real bug caught and fixed live during this pass (confirmed via `document.querySelector('meta[property="og:description"]')` before/after).
- `generateMetadata()` on the three dynamic detail pages (`resources/[slug]`, `articles/[slug]`, `community/[id]`) — real title + a description built from the resource's `description` field / stripped article HTML (`stripHtml()`) / discussion `content`.
- Static `metadata` export on every listing/static page (resources, articles, community, about, roadmap, the 3 legal pages) — previously all fell back to the generic default too.
- `alternates.canonical` on every page (previously **no canonical tags existed anywhere**), resolved against `metadataBase`.
- ~~No OG image exists~~ — **resolved 2026-07-27**: turned out `app/favicon.ico` was already a full branded 500×500 PNG (the DiplomaHub wordmark + tagline), not a generic icon — just misnamed with a `.ico` extension. Copied it straight to `app/opengraph-image.png`; Next's static-file convention auto-detects it and wires up the `og:image` meta tags site-wide, no code needed. Verified the meta tags and the served image directly. Note it's a 500×500 square, not the "recommended" 1200×630 — should still render fine on most platforms (Facebook/Twitter/Slack/iMessage all handle square OG images), just not edge-to-edge; a proper 1200×630 crop of the same logo is a nice-to-have, not done.

**JSON-LD structured data** — new shared `components/JsonLd.tsx`, rendered as `<script type="application/ld+json">` per Next's documented pattern. `WebSite` schema sitewide (in root layout); `Article` (resources' pages get `LearningResource`, discussions get `DiscussionForumPosting`) on the three detail-page types, with real author/datePublished/description pulled from the actual row.
**Security note, don't remove**: resource/article/discussion titles and content are user-submitted, so `<JsonLd>` escapes `<` (`.replace(/</g, "\\u003c")`) before injecting via `dangerouslySetInnerHTML` — Next's own documented mitigation for this exact XSS-via-structured-data class of bug, same category as the notification-email HTML-injection fix from the 2026-07-21 hardening pass.

**Still open (Search Console, dashboard only, no code)**: submit `sitemap.xml` under Sitemaps, request indexing for key pages via URL Inspection. Domain ownership verification itself is done (see OAuth section above — same Search Console property serves both purposes).

**Update 2026-07-23 (later session)**: the pervasive `<h1>`-misuse issue flagged here as its own multi-session cleanup is now resolved — see Known Gaps (moved there, no longer applicable to this SEO pass specifically).

## Environment quirks for whoever's driving

- **Update, 2026-09 session: preview/dev-server tooling actually worked fine most of the time** — the earlier "unreliable/blocked" note above was accurate for its session but shouldn't be assumed going forward. `preview_start`/`curl` against the local dev server were used successfully many times this session for real verification (header checks, CSP checks, logged-out page behavior, network-level popup/CSP debugging). The one recurring friction: when the **user's own** `npm run dev` is also running, starting a preview can hit a port-3000 conflict or a stale/confusing browser tab state — if a preview command seems to hang, gets denied, or a browser tab shows suspiciously stale content (console errors from before a restart, etc.), try a genuinely fresh tab before concluding the tooling is broken. Also: the user sometimes actively runs and tests changes in their *own* dev server in parallel — if a `preview_start` call gets rejected, that's likely why; don't retry, just ask or proceed via `tsc`/`eslint` instead.
- The user (or a linter) frequently edits files directly between turns — system reminders flag this; treat it as intentional, never revert without being asked, re-read before assuming stale state. This happened constantly in the 2026-09 session (e.g. `components/home/top-contributors.tsx` layout tweaks appeared mid-session without Claude having touched it).
- Schema/RLS/column-grant changes go through the user via raw SQL in the Supabase SQL editor — there's no direct DB access tool. Useful trick used repeatedly this session for *diagnosing* data shape/counts without a DB tool: `curl` the Supabase REST endpoint directly with the anon key from `.env.local` (safe — it's a public, client-embedded key by design, read access is governed by RLS same as the browser) — e.g. `curl "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/resources?select=...&col=eq.value" -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY" -H "Authorization: Bearer $NEXT_PUBLIC_SUPABASE_ANON_KEY"`. This is how the exact EE/TOK-exemplar migration SQL and the roadmap backfill SQL were made precise (per-row `UPDATE ... WHERE id IN (...)`) instead of guessed from a screenshot.
- **Supabase's Auth Redirect URLs allowlist has caused two separate real production bugs now** (missing `/auth/callback` broke Google login; missing `/auth/reset-password` broke password reset) — any *new* route under `/auth/` (or a new VS Code tunnel domain) needs a corresponding entry there or `redirectTo`/OAuth redirects silently fall back to the Site URL instead of erroring loudly. See "Deployment & Google OAuth verification" for the full story and the suggested wildcard fix (`/auth/**`) that would prevent this recurring a third time.
- `.claude/` (local tool permissions + preview-server launch config) is gitignored, not committed — personal machine config, not shared repo state.
- **`node_modules` gets corrupted by what looks like iCloud Drive syncing the Desktop folder mid-`npm install`** — literal duplicate directories/files named `<name> 2` (macOS conflicted-copy naming) showed up scattered across `node_modules` (`@types/estree 2`, `@supabase/auth-js/dist 2`, etc.) after installing new packages, and caused a real `tsc` failure (`Cannot find type definition file for 'estree 2'`). If `tsc`/build errors reference a package name with a stray ` 2`/` 3` suffix, this is why — `rm -rf node_modules && npm install` is the fix (node_modules is gitignored and fully regenerable, safe to nuke). Worth checking `find node_modules -maxdepth 4 -regex '.* [0-9]$'` after any `npm install` in this project until/unless the user excludes this folder from iCloud sync.
- **`.next` (the build cache) also independently got corrupted at least twice in the 2026-09 session** — `tsc`/build errors referencing `.next/dev/types/routes.d.ts` with syntax errors (`Expression expected`, `Unterminated template literal`) were the symptom both times, most likely from starting/stopping preview dev servers in quick succession. `rm -rf .next` is the fix — same as `node_modules`, it's gitignored and fully regenerable, safe to nuke, no need to ask first.
- **No admin login credentials have been available to Claude for the entire life of this project** — every admin-only feature (the whole `/admin` panel, including the new analytics dashboard) has only ever been verified via `tsc`/`eslint`, isolated logic tests of pure functions, and reasoning about the code — never actually seen rendered with real admin auth. Keep flagging this explicitly rather than implying something's been visually confirmed when it hasn't.
- **`read_console_messages` (the browser-preview tool) appears to accumulate stale console output across navigations within one long tab session**, not just the current page's live errors. Concretely reproduced 2026-09-16 (later session): after deleting `components/home/trending.tsx` etc. and confirming via `tsc`/`eslint`/screenshots/`get_page_text` that the new homepage rendered correctly, the tool still reported "Module not found: trending.tsx" and other errors describing the *old, already-deleted* `app/page.tsx` content, repeated dozens of times. Screenshots and `get_page_text` reflect real current state reliably; this specific tool's error output does not always. If its errors contradict what a screenshot plainly shows working, trust the screenshot, and cross-check via `read_network_requests` (request statuses) rather than assuming the app is actually broken.
- **The user sometimes rewrites entire pages of copy directly themselves mid-session** (not just small tweaks) — e.g. `app/legal/privacy-policy/page.tsx` was substantially rewritten by the user during the 2026-09-16 later session (new Hub/calendar-import/calendar-link sections, renumbered sections 5 onward, new "Last updated: 16/09/2026" date) without Claude touching it. Uncommitted as of that session's end. Same rule as the general "edits appear between turns" note above — don't revert it, and it's worth actually reading before assuming a diff is stale/wrong.

## Session log

- **2026-07-13**: Committed the mobile responsiveness pass (`97c9503`) and the admin feedback panel (`0145952`) that had been sitting uncommitted across prior sessions — 33 + 5 files. Found and fixed a real bug in the process: the mobile-pass diff wired `ShareButton` into resource/article/community/profile detail pages, but `components/shareButton.tsx` itself was untracked, so that commit didn't build in isolation until a follow-up commit (`0e3a43c`) added the missing file. `tsc --noEmit` is clean post-commit. User confirmed the `award_points()` 500→1000 threshold SQL has been run. **Still open:** the mobile pass has not been visually verified in a browser/mobile viewport by Claude — do that before considering it done.
- **2026-07-21**: Added the roadmap admin panel, notifications system (in-app + Resend email, currently disabled), then a full pre-launch hardening pass — see that section above for the complete breakdown. Highlights: fixed a real bug where `middleware.ts` sat in the wrong directory and had literally never run; added server-side validation and file-upload validation everywhere; rewrote all three main listing grids (Resources/Articles/Community) from "fetch everything, slice client-side" to real server-side pagination, including two new Postgres RPC functions for Community's computed "Hot" sort; added DB indexes, error boundaries, Upstash rate limiting, and Sentry error tracking. Full-project `tsc --noEmit` and `eslint` both clean throughout (verified after every file group, not just at the end). **Not verified in a browser this session** — user declined preview/browser checks each time it was offered; everything here is unverified beyond type-checking and linting until manually tested, especially the three rewritten grids and their filter/sort/pagination interactions. **Blocking on user action**: two SQL scripts unrun, three env vars unset (Upstash, Sentry, and confirming Resend domain) — see "Known gaps" for the full list.
- **2026-07-23**: Long session, site now live in production. User confirmed the 2026-07-21 SQL/env-var items are done and Google OAuth consent screen verification passed. Debugged and fixed a real production bug in the new forgot-password flow (Supabase Redirect URLs allowlist missing `/auth/reset-password` — same root cause class as an earlier OAuth login bug) and added a friendly homepage banner for expired/reused email links. Then ran a full "post-launch review" pass covering everything a launched site actually needs: fixed a real broken-layout bug (`profile/edit`'s `px-[600px]`), added security headers (CSP without nonces — deliberately avoided forcing dynamic rendering sitewide), built the forgot-password/reset-password flow from scratch (previously didn't exist at all), rate-limited the two engagement actions (like/save) that had been missed in the first rate-limiting pass, added `sitemap.ts`/`robots.ts`, added a GitHub Actions CI pipeline (typecheck+lint), and fully cleaned the 38-problem lint baseline to make that CI pipeline actually mean something — found two real bugs in the process (`components/data.ts` was 557 lines of fully orphaned pre-database seed code; `LikeButton`/`SaveButton`/`ShareButton` silently ignored a `size` prop several call sites were actually passing). Fixed a completely dead "View Full Profile" button on resource pages (plain `<button>`, no `href` at all). Added a French subject category. Did a full SEO pass: per-page metadata (every page previously shared one identical generic title — a real, meaningful gap for a site with `sitemap.ts` already enumerating individual content pages for Google to crawl), canonical URLs (none existed anywhere before), and JSON-LD structured data (with a deliberate XSS-escaping safeguard since the data includes user-submitted content). Scoped (but did not implement) a large, genuine `<h1>`-misuse cleanup — 294 occurrences across 46 files, ~71% not actual page headings, zero `<h2>`/`<h3>` anywhere in the codebase — phased plan is in Known Gaps for whoever picks it up. Everything this session was verified either live in the browser (screenshots, JS-evaluated DOM checks, console/server-log checks) or via direct `curl` against the dev server, not just `tsc`/`eslint` — a change from the 2026-07-21 session where browser verification was declined throughout. `tsc --noEmit` and `eslint .` both clean project-wide at every checkpoint, confirmed clean at the end too. Commits this session: `3b05c5b` through `db937e5` (see `git log`).
- **2026-07-23 (later, separate session)**: Fixed a real production bug: resource create/edit failed with a generic "unexpected response from server" whenever a file was attached — root cause was the resource file upload going through a Next.js Server Action, which on Vercel is capped by the platform's serverless function payload limit (~4.5MB) regardless of `next.config.ts`'s `serverActions.bodySizeLimit` (that setting only governs self-hosted Next). Fixed by uploading resource files directly from the browser to Supabase Storage (`UploadResourceForm.tsx`), bypassing the server action entirely for the file bytes — new shared `app/lib/uploadValidation.ts` holds the validation logic used both there and by the remaining server-side upload actions (avatar/cover, which stay server-side since they're well under the limit). Also added a shared `Spinner` component and wired it into every form/delete-button busy state that previously showed no loading feedback at all (comment/reply delete, admin tables) or only a text change. Then executed the full `<h1>`-misuse cleanup that the earlier same-day session had scoped but not started — see Known Gaps, now resolved, 294 → 25 occurrences across 6 commits (`209a1f1` through `42726f8`). Everything verified in-browser at each phase; `tsc --noEmit` and `eslint` clean throughout.
- **2026-07-27**: Long session, mix of production bug fixes, a real backend-formula bug, and several feature requests. In rough order: (1) discovered `app/favicon.ico` was secretly a full branded logo PNG and used it directly as `app/opengraph-image.png` — no OG image had existed before. (2) Found and fixed the actual reason loading spinners from the previous session's spinner pass never visibly appeared: `<form action={plainClientFn}>` runs as a React 19 transition, which can skip painting intermediate state entirely if the whole submit resolves fast enough — switched every such form (login, signup, forgot/reset password, profile edit, resource upload/edit, write article, write discussion) to a plain `onSubmit` handler instead; see "Established code patterns" for the lesson. (3) At the user's request, added a second permissive RLS UPDATE policy to `resources`/`articles`/`discussions` so admins can edit any content, not just their own — confirmed run. (4) Built full account deletion (self-service Danger Zone + admin delete button), backed by new `ON DELETE CASCADE`/`SET NULL` foreign keys the user chose ("delete everything" over anonymizing) — SQL handed over, not explicitly confirmed run. (5) Diagnosed a real bug in `calculate_community_trust()`: its Bayesian-shrinkage blend could make trust *decrease* as downloads increased whenever the resource's own engagement score was below its author's prior — fixed with a floor (`greatest(author_score, blended)`); user considered but declined a bigger architecture change (pure-engagement score + computed author reputation) in favor of keeping this floor-patched version. A follow-up report ("still 50% after 10 downloads") matches expected post-fix behavior but the underlying question of whether the formula's sensitivity (`confidence_k = 20`) is well-tuned was left open, interrupted before resolution — see "Community trust score formula" and Known Gaps. (6) Removed DP1/DP2 from `YEAR_OPTIONS` per the user's request (user had already reassigned existing DB rows). (7) Fixed a second real production bug reported with an (incorrect) AbortController-based bug report: rapid filter/sort/pagination clicks on the three main grids fired unbounded concurrent Server Action POSTs and could fail under Vercel's concurrency limits — AbortController isn't actually available for direct Server Action calls in this Next version (verified against the client runtime source), so debounced the fetch trigger instead; verified 5 rapid clicks → 1 request. (8) Added real server-side pagination to all three admin tables (Users/Content/Feedback), which had been missed by the 2026-07-21 hardening pass since the admin panel didn't exist yet at that point; extracted a shared `Pagination` component. (9) Homepage "Featured Resources" now ranked by the same hot-score shape as Community's Hot sort, capped to one resource per subject via a new `DISTINCT ON`-based Postgres RPC, with a safe fallback to the old newest-first query — SQL handed over, not confirmed run. (10) Small polish: Top Contributors leaderboard rows now link to `/profile/{id}`; the community page's "All Subjects" dropdown shows a live subject count. Everything code-side is committed and pushed to `features`; verified via clean `tsc`/`eslint` throughout plus real browser testing for the debounce fix and the leaderboard links (screenshot tool had an unrelated rendering hiccup once, worked around via DOM/network inspection instead). **Three SQL scripts from this session are unconfirmed as run** — see Known Gaps for the full list and what breaks/degrades gracefully if they haven't been.
- **2026-09-15 (a very long session — this entry covers everything since the 2026-07-27 one above; heavy context-compaction across it means some intermediate dates/exact sequencing weren't retained, so this is organized by topic rather than a strict timeline)**: Broad mix of new features, a real Vercel cost crisis, and several production bug fixes. By topic:
  - **Resources — file/link model rework**: a resource can now carry a file, a link, or both (previously either/or via a `type_tag === "External Link"` special case) — new `resources.link_url` column, migration confirmed run and verified live. Later, `type_tag`'s generic `"Exemplar"` was split into `"Exemplar - IA"/"Exemplar - EE"/"Exemplar - TOK"` (with a synthetic "Exemplar" browse-filter option matching all three) so EE/TOK exemplars could be re-tagged to their real subject instead of being stuck in a subject-less `"EE"`/`"TOK"` bucket — full detail in "Data model" and "Search" sections above. Added a `"Theatre"` subject. Renamed `"Past Paper Tips"` → `"Past Paper"`.
  - **Search**: keyword (not substring) search added to Resources, Articles, and Community — see "Search" section above, including a real production incident (`CREATE OR REPLACE FUNCTION` leaving a stale-signature RPC behind and breaking `getTrendingDiscussions()`) and its fix.
  - **Leaderboard & retention**: new `/leaderboard` page, points linked to it from everywhere points are shown, profile pages show numeric rank, homepage got a "Top Contributors" section — see "Leaderboard" section above. Separately, `users.last_active_at` retention tracking was added (see "Retention tracking" section) directly in response to "how do I know if visitors come back," designed for near-zero cost given the CPU-cost context below.
  - **Roadmap**: went from "status/completion editable, everything else hardcoded" to fully DB-driven admin CRUD (create/edit/delete, every field) — see "Roadmap" section above.
  - **Admin analytics dashboard**: `/admin` went from 4 count cards to a real dashboard with 30-day trend charts, a most-downloaded leaderboard, subject/type distribution charts, and the new Active Users stat — hand-written SVG charts (no new charting dependency), built using the `dataviz` skill's validated-palette methodology since the app's actual brand colors fail chart-safety checks. See "Admin analytics dashboard" section above.
  - **Vercel Fluid CPU cost crisis**: the user's Hobby-plan Fluid Active CPU quota hit 100%. Real investigation across multiple dashboard-data round-trips with the user (not guess-and-check) — see the dedicated "Vercel Fluid CPU cost investigation" section above for the full story, including one wrong-then-corrected hypothesis (`next.config.ts` headers matching every static asset — real, but not the dominant cause) and the actual dominant one (the auth middleware and every single page both independently re-validating the session against Supabase's Auth server, fixed by having the middleware forward its result downstream via a header). Also fixed `NotificationBell`'s unconditional 30s poll to pause when the tab is hidden. A `@vercel/analytics`/`@vercel/speed-insights` addition was built, then fully reverted at the user's request once they enabled Web Analytics directly in the Vercel dashboard instead.
  - **Bug fixes, smaller but real**: a Safari-specific popup-blocker bug in the "Visit Link" download flow (`window.open()` after an `await` — Safari-specific, see "Established code patterns"); a resource-card title/description visual-overlap bug with a genuinely subtle root cause (`line-clamp` + explicit height fighting each other, see the Resources feature-status entry); a login-required toast that was getting clipped by a card's `overflow-hidden` (fixed via `createPortal` to `document.body`); mobile navbar icon/text sizing polish and a notification-dropdown mobile-viewport overflow fix (both requested directly by the user while screen-sharing/testing).
  - **Discussed, not built**: a donation/support-us feature and its interaction with a future paid Diploma Pro tier — recommendation given, no decision made, no code written. See Known Gaps.
  - Everything code-side is committed and pushed to `features`. Verification this session leaned heavily on `tsc --noEmit`/`eslint` (both clean throughout) plus real browser testing via `preview_start`/`curl` for anything reachable without login (search, mobile layout, CSP headers, Safari-popup logic, spoofed-header security checks for the auth changes) — **the admin panel, including the entire new analytics dashboard, has still never been visually verified by Claude** (no admin credentials available, a standing constraint for the life of this project — see "Environment quirks"). Several SQL scripts from this session (the type/subject re-tagging, the `last_active_at` column+grant) are handed over but not explicitly confirmed run — see Known Gaps for the full list.
- **2026-09-16**: Built the Hub planner (`/hub`) from scratch — see the dedicated "Hub planner" section above for the full breakdown, this is just the session narrative. **Work happened on a new `thehub` branch (off `features`), nothing from this session is committed yet** — first thing a follow-up session should check is `git status`/`git log` on that branch before assuming anything below is saved. Two clearly separated phases: (1) a pure client-side design prototype — weekly drag/resize calendar, milestones bar, Pomodoro timer, study-time log, all mock data, zero DB — iterated on with the user via direct back-and-forth bug reports (a hover-triggers-drag bug, a cross-day-drag React remount bug that silently broke commits while *looking* like it worked, text-selection-during-drag, a locale-formatting hydration mismatch on `/hub` that turned out to be a pre-existing site-wide issue unrelated to Hub); (2) once the design was approved, wired to real Supabase persistence (new `hub_items`/`hub_study_log` tables, `app/lib/actions/hub.ts`) plus a brand-new 4-step onboarding wizard with a CSS-only "spotlight tour" effect (dims the page, cuts a bright animated hole around whatever the current step is talking about) and full guest-mode support (complete the whole flow with zero DB calls, migrate into a real account on sign-in). The subject list grew twice mid-session at the user's direction: first 8 synthetic subjects → all 16 real resource-taxonomy subjects (to reuse for real "Recommended Resources" lookups), then a 6-subject cap with TOK/EE/CAS/General as free pre-checked "core" picks (matching a real DP course load) after the user pointed out every IB student already has those four. **The one thing this session could not verify**: the signed-in persistence path — no test account was available, so it's typecheck/lint-clean and code-reviewed but not seen actually hitting `hub_items` in a browser. The guest path (same UI, localStorage backend) *was* fully exercised live, including the spotlight tour, drag/resize correctness, and the subject cap. **Next session: run the three SQL blocks in "Hub planner" above, then do a real signed-in pass** — reload-persistence, the guest→account migration effect, and confirm the onboarding wizard gates correctly on `hub_onboarded_at` instead of always reappearing.
- **2026-09-16 (later session, same day)**: Long follow-on session, still on `thehub`. Ran the three originally-planned Hub phases (Today's Focus strip, ICS calendar feed subscription, `.ics` file import — see the "Update, 2026-09-16 (later session)" note inside "Hub planner" above for the full breakdown of each), then kept going through several more rounds of direct user feedback: a full-screen Pomodoro focus mode, two real mobile/tablet layout bugs found from the user testing an actual Vercel preview on their iPad and phone, a subject-defaults change (new accounts now start with zero real subjects picked, core-only, instead of all 16), and a new editable "graduating session" picker in the Hub header. Then, in a separate request, redesigned the whole homepage in plan mode (user explicitly asked to see the plan before any editing) to remove discussions sitewide and make `/` Hub/resources-focused instead — full breakdown in the new "Homepage redesign" section above. Two real, non-obvious bugs were caught and fixed *during this session* rather than shipped and found later: a partial-unique-index/`ON CONFLICT` bug in the `.ics` import upsert (reported live by the user against their preview deploy, root-caused and fixed same session), and this app's `--spacing-*` custom-token collision with bare Tailwind size utilities (`max-w-xl`/`max-w-md`) biting three separate times across the session in three different components — now called out explicitly in "Established code patterns" territory so it stops recurring. Committed and pushed to `features` twice mid-session (the three-phase Hub build + subject-defaults + session-picker as one commit, then the two mobile-layout fixes as a second) — **but the entire homepage redesign that came after is uncommitted**, sitting in the working tree alongside the user's own unrelated manual rewrite of the privacy policy. **Genuinely never verified signed-in this session either** — every single piece built today (focus mode, Today's Focus, calendar sync, `.ics` import, the new dashboard) still needs a real logged-in pass; no test account has been available to Claude for the life of this project. See "Known gaps" for the complete, currently-open checklist this hands off to whoever picks this up next.
